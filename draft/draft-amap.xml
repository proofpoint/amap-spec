<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE rfc [
  <!ENTITY nbsp    "&#160;">
  <!ENTITY zwsp   "&#8203;">
  <!ENTITY nbhy   "&#8209;">
  <!ENTITY wj     "&#8288;">
]>
<rfc xmlns:xi="http://www.w3.org/2001/XInclude" ipr="trust200902" docName="draft-amap-00" category="info" submissionType="independent" tocInclude="true" sortRefs="true" symRefs="true" version="3">
  <front>
    <title abbrev="AMAP">The Agent Mailbox Access Protocol (AMAP)</title>
    <seriesInfo name="Internet-Draft" value="draft-amap-00"/>
    <author initials="D." surname="Rapp" fullname="Daniel Rapp">
      <organization>Proofpoint, Inc.</organization>
      <address>
        <email>drapp@proofpoint.com</email>
      </address>
    </author>
    <author initials="T." surname="Adams" fullname="Trent Adams">
      <organization>Proofpoint, Inc.</organization>
      <address>
        <email>tadams@proofpoint.com</email>
      </address>
    </author>
    <date year="2026" month="September" day="16"/>
    <abstract>

<t>The Agent Mailbox Access Protocol (AMAP) is a wire contract, carried over a
filesystem namespace, between an untrusted per-agent connector and a trusted,
credentialed mail runtime. It gives a sandboxed AI agent the ability to send
and receive email through a runtime that does not trust it: every outbound
message begins as an inert request file that the runtime copies into private
staging, validates independently, and applies policy to before anything is
transmitted, and every inbound message is authenticated and screened by the
runtime before a notice describing it appears in the agent's namespace. This
document specifies the directory layout and its filesystem obligations —
ownership, write and delete rights, and the path, symlink, and concurrency
discipline a runtime observes; the JSON message shapes for submit requests,
results, deliver notices, spooled message bodies, and an optional binding
record; the correlation and versioning rules, including a direction-aware
unknown-member tolerance rule; and two conformance classes. AMAP is
deliberately not a mail protocol: it neither defines nor replaces SMTP, IMAP,
or provider-side infrastructure, and composes with them rather than
displacing them. Its single design center is that no security property
depends on the agent — or on the connector speaking for it — behaving
correctly.</t>
      <t><strong>Naming.</strong> This protocol was called AMP ("Agent Mailbox Protocol") until
2026-09-15. It was renamed because AMP collides with the IETF DTN working
group's adopted <tt>draft-ietf-dtn-amp</tt>. The filename of this draft is unchanged
at this document version; the schema identifiers moved with the name (Change
Log, 3.1.0).</t>
      <!-- NOT `note_Status_of_This_Memo`. That heading is RESERVED: with a real `ipr`
     value xml2rfc generates the true "Status of This Memo" itself, and a note
     of the same name renders BESIDE it. The two then contradict each other on
     precisely the question the IPR value exists to answer — the generated text
     says the draft "is submitted in full conformance with the provisions of
     BCP 78 and BCP 79", ours says it is not a standard and nothing has been
     submitted. Under `ipr: none` no boilerplate was generated and the collision
     could not appear, so this was invisible until the IPR value was settled. -->



    </abstract>
    <note>
      <name>About This Draft</name>

<t>This document is an initial draft, distributed for review and comment. It is
<strong>not</strong> a standard, and no assurance is given that it will become one. It may
be updated, replaced, or rendered obsolete by later documents at any time, and
it is inappropriate to cite this draft as a stable reference.</t>
      <t>This draft describes Agent Mailbox Access Protocol wire major version <tt>2</tt>,
document version 3.1.0 (DRAFT). As of document version 3.0.0 these two numbers
deliberately track different things, and are allowed to diverge: the wire
major tracks whole-document acceptance compatibility, while the document
version tracks the fuller set of obligations placed on implementations.
<xref target="sec-10-1"/> states the rule and the reasoning. Document version 3.1.0 is
marked DRAFT: it is an additive minor revision of 3.0.0 (<xref target="sec-10-1"/>, Change
Log). The peer-origin profile it introduces is present in this document as its
schema and conformance fixtures (<xref target="app-f"/>, <xref target="app-h"/>); the profile's prose
is a companion document (<tt>spec/peer-origin.md</tt>, v3.1.0 DRAFT), and nothing in
this document's body specifies the <tt>peer/</tt> tree beyond what that schema and
those fixtures pin.</t>
      <t><strong>Change control.</strong> A capability change to this protocol — a new field, a new
message kind, or a change to an existing obligation — is a change to <em>this
document and to its conformance fixture suite</em> (<xref target="app-g"/>, <xref target="app-h"/>), made
<strong>before</strong> any implementation adopts it. No implementation introduces a
wire-visible change unilaterally: a change that is not reflected here and in
the fixtures is not part of the protocol.</t>
      <t>This document and the accompanying schemas and conformance fixtures are
published under the Apache License 2.0.</t>
    </note>
  </front>
  <middle>

<section anchor="sec-1">
      <name>Introduction — Motivation and Problem Statement</name>
      <t>AI agents increasingly need email. They are tasked over it, they return
results over it, and they correspond over it with people and with systems
that speak no other protocol. An agent is nonetheless an untrusted process: it
can be prompt-injected by the very content it is asked to read, it can be
defective, and it can be compromised outright. Granting such a process email
capability directly is dangerous along three concrete axes.</t>
      <t><strong>Inbound.</strong> Delivered mail is untrusted content routed directly into the
agent's reasoning. A message body, a subject line, a sender display name, or
an attachment filename can carry instructions the agent was never meant to
follow. The delivery path is therefore an injection vector by construction,
and no amount of care inside the agent removes that property.</t>
      <t><strong>Outbound.</strong> An agent that can send mail can exfiltrate anything it has
read, to any address it chooses, and can do so under an identity that
recipients associate with its operator. Outbound mail is simultaneously a
data-loss channel and an impersonation channel.</t>
      <t><strong>Credentials and identity.</strong> Any mailbox credential, API token, or send
endpoint held inside the agent's sandbox can be read, copied, or abused by
whatever compromises the agent. Worse, an agent holding a credential has no
identity of its own that a recipient — or its own operator — can verify
independently of that credential.</t>
      <section anchor="sec-why-the-existing-options-are-insufficient">
        <name>Why the existing options are insufficient</name>
        <t>Handing the agent mailbox credentials, or a mail-send API, makes the agent a
<em>trusted</em> participant in the mail system. That is precisely the wrong posture
for a component whose defining risk is that it may be compromised: the trust
must be withdrawn exactly when it is least possible to detect that it should
be. Wrapping the credential in a helper process that the agent calls does not
change the analysis by itself, because a callable send capability is still a
capability the agent holds; what matters is whether the capability crosses
the boundary at all.</t>
        <t>The common alternative is per-deployment glue: a bespoke script or service
wired between one agent harness and one mail system. Such glue may even be
correct, but it produces no reviewable contract, no interoperability, and no
shared conformance bar. Nothing tells a second implementer what the first one
guarantees, and nothing tells either of them what a third-party reviewer
should check.</t>
        <t>Adjacent agent protocols do not fill the gap, because they are solving a
different problem: integration and interoperation <em>among credentialed
participants</em>, rather than isolation of an <em>untrusted</em> one. <xref target="sec-3-4"/>
positions AMAP against that landscape in detail.</t>
      </section>
      <section anchor="sec-the-approach">
        <name>The approach</name>
        <t>AMAP interposes a <strong>trusted runtime</strong> between the agent and the mail system.
The runtime holds every credential, owns the agent's sending identity, and
makes every policy decision. The agent interacts with the runtime only
through a channel that carries <strong>no capability</strong>: it writes an inert request
file into a directory, and the runtime — independently, on its own schedule,
over its own private copy of those bytes — decides what, if anything, happens
next. In the other direction the runtime authenticates and screens mail
before writing a notice describing it into the agent's namespace, and
withholds the bytes of anything it will not vouch for.</t>
        <t>The payoff, which the rest of this document formalizes, is the load-bearing
invariant:</t>
        <ul empty="true">
          <li>
            <t>No security property depends on the agent — or on the connector speaking
for it — behaving correctly. Enforcement is entirely runtime-side. A
compromised agent or connector can at most write a well-formed request that
the runtime then refuses.</t>
          </li>
        </ul>
        <t>AMAP is the wire contract at exactly that trust boundary, and is deliberately
owned by neither side of it. That ownership choice is what allows a runtime
and a connector built by different parties, on different schedules, to
interoperate and to co-evolve against a versioned contract with an offline
conformance suite.</t>
      </section>
      <section anchor="sec-why-a-filesystem-namespace-rather-than-an-api-rpc-or-queue">
        <name>Why a filesystem namespace rather than an API, RPC, or queue</name>
        <t>The medium <em>is</em> the boundary. An API, an RPC endpoint, or a message queue is
a live capability: something the agent invokes, whose invocation is itself
the grant. Its security therefore rests on authenticating and authorizing
each call, on the client library behaving, and on the transport being what it
claims to be. A file written into a directory grants nothing at all. It has
no callee, no session, and no side effect until a separate, trusted process
chooses to read it — at a time of that process's choosing, into memory of its
own, under validation of its own. The runtime is free to ignore the file, to
refuse it, to hold it for human review, or to consume it and answer with a
verdict. <xref target="sec-3-3"/> develops this point, along with the obligations it puts
on the runtime in exchange.</t>
      </section>
      <section anchor="sec-design-goals">
        <name>Design goals</name>
        <ul spacing="normal">
          <li>
            <t><strong>Capability-free isolation.</strong> The channel available to an untrusted agent
grants no send capability, no credential, and no reach outside its own
namespace.</t>
          </li>
          <li>
            <t><strong>Vendor-neutral interoperability.</strong> A runtime and a connector built
independently interoperate because both are built against this contract
rather than against each other.</t>
          </li>
          <li>
            <t><strong>Versioned co-evolution with offline conformance.</strong> Either side proves
conformance against a fixture suite with no counterpart present, and
version changes are deliberate, legible, and fail closed rather than
silent.</t>
          </li>
          <li>
            <t><strong>Composition with real email.</strong> Interactions remain federated, of-record,
and human-inclusive, rather than confined to a closed agent-only channel.</t>
          </li>
          <li>
            <t><strong>Portability across agent runtimes.</strong> Nothing in the contract presumes a
particular agent harness, language, or tool interface.</t>
          </li>
        </ul>
      </section>
      <section anchor="sec-non-goals">
        <name>Non-goals</name>
        <ul spacing="normal">
          <li>
            <t><strong>AMAP is not a mail transport.</strong> It neither defines nor replaces SMTP,
IMAP, or any provider-side mail infrastructure. It describes what a runtime
and a connector exchange <em>locally</em>; whatever the runtime does with a mail
provider is out of scope.</t>
          </li>
          <li>
            <t><strong>AMAP is not an agent-to-agent networked RPC.</strong> The contract is local to
one deployment's filesystem, between two components on either side of a
single trust boundary. It defines no network protocol, no discovery
mechanism, and no remote peer.</t>
          </li>
          <li>
            <t><strong>AMAP is not a policy language.</strong> What a runtime's policy decides — which
recipients are permitted, which content is held for review, how a sender's
standing is computed — is deliberately out of scope. The wire defines only
the shapes of requests and verdicts, never the computation that produces
them.</t>
          </li>
        </ul>
        <t>Every obligation in this document is grounded in what the contract actually
provides. Where a capability is sketched but not in force, it is marked as
such (<xref target="app-i"/>), and where a property is a runtime obligation rather than a
wire-verifiable fact, this document says so plainly (<xref target="sec-11-14"/>).</t>
      </section>
    </section>
    <section anchor="sec-2">
      <name>Conventions and Terminology</name>
      <t>The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD",
"SHOULD NOT", "RECOMMENDED", "NOT RECOMMENDED", "MAY", and "OPTIONAL" in this
document are to be interpreted as described in BCP 14 <xref target="RFC2119"/> <xref target="RFC8174"/>
when, and only when, they appear in all capitals, as shown here.</t>
      <t>Every message defined by this document is a JSON text as defined by
<xref target="RFC8259"/>, encoded in UTF-8 <xref target="RFC3629"/>. The schemas in <xref target="app-a"/> through
<xref target="app-e"/> are written in the JSON Schema 2020-12 dialect
<xref target="JSON-SCHEMA-2020-12"/>.</t>
      <t>Sections and subsections explicitly marked <em>(informative)</em> or
<em>(non-normative)</em> carry no requirements; <xref target="sec-3-4"/>, <xref target="app-i"/>, and the
Change Log are the only such parts of this document.</t>
      <t><strong>Terminology.</strong></t>
      <ul spacing="normal">
        <li>
          <t><strong>Agent</strong> — the untrusted process whose email capability this protocol
mediates. It holds no credential, has no send path of its own, and reaches
the protocol only through its Connector. Nothing in this document places a
requirement on the Agent, because no security property may depend on it.</t>
        </li>
        <li>
          <t><strong>Connector</strong> — the per-Agent component that speaks the Agent-side half of
this contract: it writes Submit Requests, reads Results and Notices, and
surfaces them to its Agent in whatever form its harness uses. It is
semi-trusted at best; for the purpose of every security property in this
document it is treated as untrusted, and the requirements placed on it are
well-formedness and non-crashing obligations, never obligations another
party's safety rests on.</t>
        </li>
        <li>
          <t><strong>Runtime</strong> — the trusted, credentialed, Agent-agnostic component that
speaks the other half: it holds mail credentials, owns each Agent's
identity, makes every policy decision, and is the only party that
transmits or receives mail.</t>
        </li>
        <li>
          <t><strong>Volume</strong> — the runtime-owned filesystem tree carrying this protocol,
containing one Namespace per Agent plus an optional Binding Record at its
root. Its mount path is a deployment choice; this document writes
<tt>&lt;volume-root&gt;/</tt> for it and nothing in the protocol derives from its name.
No implementation may depend on that name or any other.</t>
        </li>
        <li>
          <t><strong>Namespace</strong> — the per-Agent subtree <tt>&lt;volume-root&gt;/&lt;agent-id&gt;/</tt>, mounted
into that Agent's sandbox. It is the unit of isolation and the sole
authority for attributing an artifact to an Agent.</t>
        </li>
        <li>
          <t><strong>Submit Request</strong> — the Connector-authored artifact requesting that a
message be sent (<xref target="sec-5"/>). It is inert: it carries no capability and
effects nothing by existing.</t>
        </li>
        <li>
          <t><strong>Result</strong> — the Runtime-authored verdict for one consumed Submit Request
(<xref target="sec-6"/>).</t>
        </li>
        <li>
          <t><strong>Notice</strong> (Deliver Notice) — the Runtime-authored artifact announcing that
a message was delivered to the Agent's mailbox (<xref target="sec-7"/>). It is a
pointer and summary, not the message.</t>
        </li>
        <li>
          <t><strong>Inbound Message</strong> — the Runtime-authored artifact carrying a delivered
message's body text and headers, spooled under a Notice's identifier and
fetched on demand (<xref target="sec-7-3"/>).</t>
        </li>
        <li>
          <t><strong>Binding Record</strong> — the optional, informational artifact at the Volume
root naming which mailbox the Volume is bound to (<xref target="sec-9"/>).</t>
        </li>
        <li>
          <t><strong>Sidecar</strong> — attachment bytes stored beside a Submit Request or a Notice,
in a directory named after that artifact, with each blob named by a
0-based ordinal.</t>
        </li>
        <li>
          <t><strong>Ordinal</strong> — the integer naming a Sidecar blob. It equals the index of the
corresponding Descriptor in its array, and is the sole binding authority
between a Descriptor and its bytes.</t>
        </li>
        <li>
          <t><strong>Descriptor</strong> — one entry in an <tt>attachments[]</tt> array, describing an
attachment (its display filename, media type, size, and — depending on
direction — digest, Disposition, or content reference). A Descriptor is
metadata; it is never a path.</t>
        </li>
        <li>
          <t><strong>Disposition</strong> — the Runtime's assertion about an inbound attachment's
handling: <tt>clean</tt>, <tt>stripped</tt>, <tt>quarantined</tt>, or <tt>unscanned</tt> (<xref target="sec-7-2"/>).</t>
        </li>
        <li>
          <t><strong>Staging</strong> — Runtime-private storage outside the Volume, unreachable from
any Agent's mount, into which the Runtime copies Agent-authored bytes
before acting on them (<xref target="sec-4-1"/>).</t>
        </li>
      </ul>
      <t>Where this document says an artifact is <em>dropped</em>, it means the consumer does
not surface it to its Agent and does not act on it; where it says a consumer
<em>fails closed</em>, it means the consumer refuses to process the artifact rather
than proceeding on a partial or guessed interpretation.</t>
    </section>
    <section anchor="sec-3">
      <name>Architecture and Trust Model</name>
      <section anchor="sec-3-1">
        <name>Actors</name>
        <table>
          <thead>
            <tr>
              <th align="left">Actor</th>
              <th align="left">Trust</th>
              <th align="left">Holds</th>
              <th align="left">Reaches</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td align="left">
                <strong>Runtime</strong> (container)</td>
              <td align="left">trusted</td>
              <td align="left">mail-send credentials, Agent identity, policy, the record of what was sent</td>
              <td align="left">the mail provider, the Volume</td>
            </tr>
            <tr>
              <td align="left">
                <strong>Connector</strong> (per Agent)</td>
              <td align="left">semi-trusted, Agent-side</td>
              <td align="left">nothing credentialed</td>
              <td align="left">its own Namespace on the Volume; its Agent's input and output</td>
            </tr>
            <tr>
              <td align="left">
                <strong>Agent</strong></td>
              <td align="left">
                <strong>untrusted</strong></td>
              <td align="left">only what the Connector surfaces</td>
              <td align="left">the Connector; its sandbox</td>
            </tr>
          </tbody>
        </table>
        <t>The Runtime is trusted by construction: it is the party holding the
credentials, and a compromise of it is a compromise of the mail capability
itself. The Connector is the interesting case. It runs Agent-side, is
reachable by whatever compromises the Agent, and is therefore treated as
untrusted for every property that protects the Runtime, other Agents, or the
outside world. Requirements this document places on a Connector fall into
two distinct classes, and the difference matters when reading them:</t>
        <ul spacing="normal">
          <li>
            <t><strong>Well-formedness and liveness obligations</strong> — emit valid documents,
tolerate a missing artifact, do not crash on hostile input. A Connector
that violates one harms itself and its own Agent, not any other party.</t>
          </li>
          <li>
            <t><strong>Obligations that protect an Agent from its own inbox</strong> — the
untrusted-content posture of <xref target="sec-7"/>. These are necessarily
Connector-implemented, because the Connector is the only component on the
Agent's side of the boundary, and they are stated as such rather than as
properties the Runtime relies on.</t>
          </li>
        </ul>
        <t>No requirement in this document places a Runtime-side, other-Agent-side, or
outside-world safety property in a Connector's hands. Where an earlier
reading of this contract appeared to do so — the Connector's obligation not
to rewrite a published Submit Request — <xref target="sec-4-1"/>'s ingest-by-copy rule
removes the dependency, leaving that obligation as pure well-formedness.</t>
      </section>
      <section anchor="sec-3-2">
        <name>The load-bearing invariant</name>
        <ul empty="true">
          <li>
            <t><strong>Invariant.</strong> No security property may depend on the Agent or the
Connector behaving. Enforcement is entirely Runtime-side. A compromised
Agent or Connector can at most write a well-formed request that the
Runtime then refuses.</t>
          </li>
        </ul>
        <t>Everything else in this document is downstream of that sentence. It is why
the Agent's declared identity is a cross-check rather than an attribution
source (<xref target="sec-4-1"/>); why the Runtime re-derives every path from data it
controls rather than from a file's contents (<xref target="sec-4-1"/>); why recipient
policy is evaluated Runtime-side over the exact recipient set that will be
transmitted to (<xref target="sec-5-1"/>); why the digest an Agent supplies for its own
attachment bytes is an integrity aid and not a trust boundary
(<xref target="sec-5-1"/>); and why the absence of bytes, rather than a flag inside a
document, is the access gate on inbound attachments (<xref target="sec-7-2"/>).</t>
        <t>The invariant also bounds what this document can promise. A wire contract
cannot make a Runtime honest, and a fixture suite cannot observe a
filesystem. <xref target="sec-11-14"/> states plainly which properties are
wire-verifiable and which are Runtime obligations that a green conformance
run does not prove.</t>
      </section>
      <section anchor="sec-3-3">
        <name>Why a filesystem namespace</name>
        <t>The Runtime mounts each Agent's Namespace into that Agent's sandbox, and the
Agent's entire protocol surface is that directory. This is a deliberate
choice of medium, with three consequences.</t>
        <t><strong>A file drop carries no capability.</strong> Writing a file invokes nothing. There
is no callee to authenticate, no session to hijack, no method whose mere
reachability is a grant. The request exists as bytes until a trusted process
elects to look at them. Compare an API or queue, where the act of enqueuing
is itself the privileged act and every security property depends on the
authorization check in front of it being correct and unbypassable.</t>
        <t><strong>Isolation is structural rather than enforced by policy.</strong> A Connector
scoped to <tt>&lt;volume-root&gt;/&lt;agent-id&gt;/</tt> cannot name another Agent's Namespace
at all. Attribution follows from <em>where a file appeared</em>, not from what the
file says about itself, so spoofing attribution requires defeating the mount
rather than lying in a JSON member.</t>
        <t><strong>The medium's costs are real and are paid explicitly.</strong> A shared filesystem
between a trusted and an untrusted party is itself an attack surface:
symlinks, directory swaps, special files, hard links, and unbounded sizes are
all available to the untrusted side, and every path-based check the trusted
side performs is racy against a concurrent rename. This document therefore
does not treat "it is just a directory" as a simplification. <xref target="sec-4-1"/>
states the path, open, copy, and concurrency discipline a Runtime observes
in exchange for the medium's advantages, and <xref target="sec-11"/> revisits each as a
threat.</t>
      </section>
      <section anchor="sec-3-4">
        <name>Relationship to Other Agent Protocols (Informative)</name>
        <t><em>This subsection is informative. It contains no requirements and creates no
dependency on any external protocol. Nothing here claims that AMAP replaces,
subsumes, or competes with any protocol named below.</em></t>
        <t>AMAP occupies a narrow position: it is the local seam at a single trust
boundary, where one side holds every credential and the other holds none.
The adjacent protocols in the agent ecosystem sit at different layers, and
the common question of "why not use one of them here" has a specific answer
in each case.</t>
        <t><strong>Model Context Protocol (MCP)</strong> <xref target="MCP"/> connects an agent host to tools and
context through JSON-RPC servers. It is an integration interface: live
capabilities are exposed across it and exercised by the agent through it.
That makes MCP complementary to AMAP rather than an alternative. A Connector
often surfaces AMAP's operations to its Agent through MCP, or through
whatever tool interface its harness uses natively; AMAP is the
capability-free seam sitting behind that surface. The contrast worth stating
is exactly the one that motivates the two coexisting: an MCP-style interface
grants live capability across the boundary it spans, while AMAP's channel
deliberately grants none — the Agent-side surface can be as rich as the
harness likes, because what crosses the trust boundary underneath it is an
inert file. The current stable MCP specification revision is 2025-11-25; the
protocol originated at Anthropic.</t>
        <t><strong>Agent2Agent (A2A)</strong> <xref target="A2A"/> is networked agent-to-agent RPC between
mutually authenticated services, with agent cards, tasks, and HTTP/SSE/JSON-RPC
transport. It presumes both endpoints are credentialed participants that can
authenticate to each other. AMAP's design center is the opposite: an
untrusted agent that holds no credentials at all, with enforcement placed
entirely on the trusted side of a local boundary. The two are orthogonal, and
compose without friction — an agent that speaks A2A to its peers can use
AMAP for its mail capability, and neither protocol needs to know about the
other. A2A is at v1.0 (v1.0.1, May 2026); it originated at Google and has
been hosted as a Linux Foundation project since June 2025.</t>
        <t><strong>The two protocols abbreviated "ACP" are not the same protocol</strong>, and the
collision is worth disambiguating in a document a reviewer may arrive at
with one of them in mind.</t>
        <ul spacing="normal">
          <li>
            <t>The <strong>Agent Communication Protocol</strong> (IBM/BeeAI) is of historical interest
here: it merged into A2A under the Linux Foundation, announced in August
2025, with its repository archived <xref target="ACP-MERGER"/>.</t>
          </li>
          <li>
            <t>The <strong>Agent Client Protocol</strong> <xref target="ACP-CLIENT"/> is a distinct and currently
active protocol at a different layer — an interface between a host or
editor and an agent — originated by Zed Industries. It is unrelated to the
merged Agent Communication Protocol and unrelated to AMAP.</t>
          </li>
        </ul>
        <t>The citation facts in this subsection were verified in July 2026. A reviewer
preparing this document for publication at a later date is advised to
re-verify them rather than relying on this text.</t>
      </section>
    </section>
    <section anchor="sec-4">
      <name>Volume Layout</name>
      <t>The Runtime owns the Volume and mounts each Agent's Namespace into that
Agent's sandbox. A Connector has access <strong>only</strong> within its own
<tt>&lt;agent-id&gt;/</tt>, and only to the paths below. Within that Namespace, write
access may be narrower still: the <tt>inbound/</tt> subtree is Runtime-owned and MAY
be mounted read-only.</t>
      <t>The Volume root's own name is a <strong>deployment choice and is not part of this
protocol</strong> — nothing derives from it, and no implementation may depend on
it. <tt>&lt;volume-root&gt;/</tt> below stands for whatever a deployment calls it.</t>
      <figure anchor="ex-volume-layout">
        <name>Volume layout (illustrative — not a fixture)</name>
        <sourcecode type="text"><![CDATA[
<volume-root>/
  identity.json           # runtime/provisioner: Binding Record (S9)
  <agent-id>/
    outbound/
      req-<id>.json          # agent -> runtime: Submit Request
      req-<id>.attachments/  # agent -> runtime: Sidecar bytes,
                             #   ordinals 0 1 2 ...
      results/<id>.json      # runtime -> agent: verdict for req <id>
      processed/req-<id>.json  # runtime: consumed requests
      ext/<name>/            # connector-owned side channel a
                             #   runtime MAY read by out-of-band
                             #   agreement; <name> is a STABLE,
                             #   opaque connector id (v3.1.0, S4.1)
    inbound/
      notices/notice-<id>.json  # runtime -> agent: delivered event
      notices/<notice-id>.attachments/  # bytes asserted clean,
                                        #   ordinals 0 1 2 ... (S7.2)
      notices/processed/     # OPTIONAL - connector archive,
                             #   writable-inbound deployments only
      messages/notice-<id>.json  # runtime -> agent: full message
      cursor.json            # runtime: inbound poll cursor
    audit/
      log.jsonl              # runtime: append-only action log
]]></sourcecode>
      </figure>
      <t>There is exactly one inbound Sidecar directory —
<tt>inbound/notices/&lt;notice-id&gt;.attachments/</tt> — and both a Deliver Notice's and
an Inbound Message's <tt>content_ref</tt> resolve there. No second, message-specific
Sidecar directory exists.</t>
      <section anchor="sec-4-1">
        <name>Obligations</name>
        <t><strong>Attribution by Namespace.</strong> <tt>&lt;agent-id&gt;</tt> is Runtime-assigned and never
self-declared. Attribution is <em>the Namespace a file arrived in</em>, never an
<tt>agent_id</tt> member inside the file; that member is a cross-check only, never
the source of truth. A Runtime MUST reject a Submit Request whose
<tt>agent_id</tt>, when present, does not match the Namespace it was found in, and
MUST NOT use that member to attribute the request.</t>
        <t><strong>Isolation is structural.</strong> A Connector scoped to <tt>&lt;agent-id&gt;/</tt> cannot read
another Agent's drafts or Notices.</t>
        <t><strong>All writes are atomic.</strong> Every artifact in the tree above is published by
writing a <tt>.tmp</tt> sibling and then renaming it into place (<tt>os.replace</tt> or the
equivalent). Readers MUST ignore <tt>*.tmp</tt> entries.</t>
        <t><strong>Single writer, single drainer.</strong> At any moment a Namespace has exactly one
Connector instance writing <tt>outbound/</tt> and exactly one Runtime instance
draining it. Every concurrency-sensitive rule in this document — the atomic
<tt>.tmp</tt>-then-rename discipline above, the commit sentinel below, and the
derivation of <tt>req_id</tt> from <tt>max(pending, processed, results) + 1</tt>
(<xref target="sec-5-1"/>) — is sound only under that assumption and provides no mutual
exclusion of its own. Each side MUST ensure single-instance operation over a
Namespace by a mechanism outside this protocol, such as an OS-level lock
held for the process lifetime. A Runtime MUST NOT run two concurrent drains
of one Namespace, and a Connector MUST NOT run two concurrent writers into
one. Behavior under violation is unspecified: duplicated delivery, clobbered
published requests, and false verdicts are all reachable.</t>
        <t><strong>Filename identifiers are the authority for paths.</strong> An identifier taken
from a file's <em>contents</em> — a <tt>req_id</tt> member in a request body, for
instance — MUST NOT determine a write path. The Runtime derives identifiers
from the validated filename, on a restricted character set. <em>(This
obligation follows from a confirmed exploit in an early implementation:
trusting a file's own contents, rather than its validated filename, to name
a write path.)</em></t>
        <t><strong>Write-side path discipline.</strong> Every write, rename, directory creation, or
archive move into or within a Namespace MUST satisfy all three of the
following <strong>conjunctively</strong>. Each closes a distinct gap the other two leave
open, and none is sufficient alone.</t>
        <ul spacing="normal">
          <li>
            <t><strong>(a) Full parent-chain pinning.</strong> The write resolves its full parent
chain through pinned directory descriptors from the Namespace root —
<tt>openat</tt>, <tt>mkdirat</tt>, <tt>renameat</tt> off a descriptor the Runtime opened once
per Namespace — never by re-resolving a path string for the act itself. A
path-based containment check followed by a path-based act is a race, not a
check: the parent directory the check inspected and the parent directory
the act touches are not guaranteed to be the same object, because the
Agent can swap a parent directory for a symlink in between. Applying a
no-follow flag to the <em>final</em> path component alone does not close this gap
— it constrains only the last component, not a swapped parent — and
rename and directory-creation calls have no no-follow equivalent at all,
so a bare "writes do not follow symlinks" requirement is not sufficient on
its own.</t>
          </li>
          <li>
            <t><strong>(b) No-follow while building the chain.</strong> Each directory component that
goes into that parent chain MUST itself have been opened with
<tt>O_NOFOLLOW|O_DIRECTORY</tt> at the moment it was added to the pinned chain. A
descriptor counts as pinned only if the open that produced it refused to
follow a symlink at that step. Requirement (a) without this is equally
insufficient, and the gap is the symmetric one: nothing otherwise stops
the Runtime from <em>acquiring</em> a "pinned" descriptor by following an
Agent-planted symlink in the first place — the Agent replaces a Namespace
subdirectory with a symlink before the Runtime's once-per-Namespace open,
or before a later re-open after a restart — and a descriptor obtained that
way is pinned to the wrong object, so every write built off it inherits
the redirection no matter how faithfully (a) is followed from there.</t>
          </li>
          <li>
            <t><strong>(c) Non-following final component.</strong> The final path component MUST NOT
be followed if a symlink exists at that name: the open MUST carry
<tt>O_NOFOLLOW</tt> and MUST fail closed on <tt>ELOOP</tt> rather than writing through
whatever the name currently resolves to. Two cases differ in what else
they require, and both are covered:
            </t>
            <ul spacing="normal">
              <li>
                <t><strong>A newly created file</strong> — the <tt>.tmp</tt> sibling the atomicity rule above
already requires, and any Sidecar blob — MUST additionally be created
with <tt>O_EXCL</tt> (<tt>O_WRONLY|O_CREAT|O_EXCL</tt>, never <tt>O_TRUNC</tt> in place of
<tt>O_EXCL</tt>), failing closed on <tt>EEXIST</tt>.</t>
              </li>
              <li>
                <t><strong>An append-mode or directory-creating write that has no <tt>.tmp</tt>
sibling</strong> — <tt>audit/log.jsonl</tt> is the case this document names — cannot
use <tt>O_EXCL</tt>, because the target legitimately already exists. It MUST
instead open with <tt>O_NOFOLLOW|O_APPEND|O_NONBLOCK</tt> (or use <tt>mkdirat</tt>
with the parent pinned per (b)), MUST NOT block on a special file, and
MUST verify through the resulting descriptor (<tt>fstat</tt>, never a pre-open
<tt>stat</tt>) that the object is a regular file it may append to; it SHOULD
refuse a target whose link count exceeds 1. The non-blocking open is
load-bearing and is not a performance concern: <tt>O_NOFOLLOW</tt> does not
apply to a FIFO — a FIFO is not a symlink — and opening one for writing
blocks until a reader appears, so without <tt>O_NONBLOCK</tt> the <tt>fstat</tt> is
never reached and an Agent that plants a FIFO at this name stalls the
drain indefinitely. The first branch's <tt>O_EXCL</tt> refuses that case on its
own; the second branch has no <tt>O_EXCL</tt> to lean on and states it
explicitly. Requiring <tt>O_EXCL</tt> in the second branch would forbid
appending to an existing log at all; requiring nothing there would leave
the one write this document names that has no <tt>.tmp</tt> sibling
unprotected.</t>
              </li>
            </ul>
            <t>
Requirement (c) is needed in both of its branches because an Agent can
pre-plant a symlink at a filename the Runtime is about to write. <tt>req_id</tt>
is Agent-chosen, and every Runtime-written filename in the layout tree
above — <tt>results/&lt;id&gt;.json</tt>, the <tt>.tmp</tt> sibling of <tt>processed/req-&lt;id&gt;.json</tt>,
<tt>inbound/notices/notice-&lt;id&gt;.json</tt>, <tt>inbound/messages/notice-&lt;id&gt;.json</tt>,
<tt>audit/log.jsonl</tt> — is reachable to an Agent that can predict or observe
it. Absent (c), such a name gets its bytes written through the planted
symlink with the Runtime's own privileges, even though the parent chain
that reached the containing directory was itself correctly pinned under
(a) and (b).  </t>
            <t>
This discipline governs every Runtime-side write named in the layout tree,
not only the initial consumption of an Agent-authored artifact.</t>
          </li>
        </ul>
        <t><strong>Read-side discipline.</strong> The same rule applied to reads. When the Runtime
opens any path the Agent can influence — every <tt>req-&lt;id&gt;.json</tt>, every
Sidecar ordinal, and, in a deployment that leaves <tt>inbound/</tt> writable,
anything it later reads back from there — it MUST open without following
symbolic links at any component (per-component <tt>O_NOFOLLOW</tt>, or an
equivalently pinned directory-descriptor walk from the Namespace root,
matching the write-side rule above), MUST NOT block on a special file (a
non-blocking open), MUST verify that the opened object is a regular file
through the open descriptor (<tt>fstat</tt> and an <tt>S_ISREG</tt> test — never a
pre-open <tt>stat</tt>, which is a race), SHOULD refuse a file whose link count
exceeds 1, and MUST enforce a deployment-configured per-file size cap (a
bounded read) and a per-request Sidecar count cap. The <tt>too_large</tt> value in
the Result vocabulary (<xref target="sec-6-1"/>) presumes that cap: enforcing one is an
obligation, not an option.</t>
        <t>A Connector's one Agent-influenceable read path — resolving a <tt>notice_id</tt> to
<tt>inbound/messages/notice-&lt;id&gt;.json</tt> — MUST be built only from a
pattern-validated <tt>notice_id</tt> (the schemas pin the pattern) and MUST confine
resolution to the Connector's own <tt>inbound/</tt> tree (<xref target="sec-7-3"/>).</t>
        <t><strong>Sidecar ordinal binding.</strong> Sidecar blobs are named by Runtime-assigned
0-based ordinals equal to the Descriptor's array index, which is the sole
binding authority. Nothing else names a blob; a Descriptor's <tt>filename</tt> is
display-only and is never a path.</t>
        <t><strong>Commit sentinel.</strong> All Sidecar bytes are written — <tt>.tmp</tt> sibling, then
rename — <strong>before</strong> <tt>req-&lt;id&gt;.json</tt>; the JSON file's presence is the only
publish signal. The Runtime MUST verify that every referenced ordinal exists
and MUST re-verify size and digest before acting on the request, against the
snapshot it took at ingest (below), never against the live path.</t>
        <t><strong>Inbound Sidecar directories are keyed on the Runtime-minted notice
identifier</strong>, never the provider <tt>message.id</tt>. The provider identifier is
external and is not character-set-safe; using one as a path component was a
confirmed exploit path in an early implementation. The same rule extends to
the body artifact: <tt>inbound/messages/notice-&lt;id&gt;.json</tt> is keyed on the
notice identifier, never on <tt>message.id</tt>. Inbound bytes are published only
at <tt>inbound/notices/&lt;notice-id&gt;.attachments/&lt;index&gt;</tt>, the single Sidecar
directory named in the layout tree.</t>
        <t><strong>Runtime-private Staging lives outside the mounted Volume</strong> — at a path the
Agent's mount cannot reach at all.</t>
        <t><strong>Ingest by copy.</strong> Before validating, evaluating policy on, or composing
from any Agent-authored artifact — <tt>req-&lt;id&gt;.json</tt> and every Sidecar
ordinal — the Runtime MUST copy the bytes into Runtime-private Staging and
MUST act only on that private copy. All checks — schema validation,
recipient policy, size and <tt>sha256</tt> verification — and all message
composition MUST run over that snapshot, and the Runtime MUST NOT re-read
any Agent-writable path after a decision that depends on its content.
Verify-then-use against a live Agent-writable path is a race, not a check.</t>
        <t>This obligation is what makes the Connector's "MUST NOT delete or rewrite a
published request" (<xref target="sec-4-2"/>) a pure well-formedness rule: a Runtime MAY
treat an observed post-publish change as malformed, but no safety property
depends on the Connector honoring immutability — which is what the
invariant of <xref target="sec-3-2"/> requires. (This governs the properties that
protect the Runtime, other Agents, and the outside world. Obligations that
protect an Agent from its own inbox are necessarily Connector-implemented
and are stated as such, not as properties the Runtime relies on a Connector
for.)</t>
        <t><strong>Byte-absence is the gate.</strong> The Runtime MUST NOT place non-clean bytes in
an Agent's Namespace. <tt>content_ref</tt> is the Runtime's access grant, present
only for bytes it has published as clean (<xref target="sec-7-2"/>).</t>
        <t><strong>Tree rules.</strong> Every Runtime-owned inbound-class tree is an <strong>origin
class</strong>: a consumer derives the class of a document from the tree it was
read from, and never from any member of the document. <tt>notice_id</tt> uniqueness
is per tree, and where a tree has more than one writer each MUST mint so
that cross-writer collision is negligible — at least 128 bits of entropy
under the identifier character set, or a writer-specific prefix — and a
consumer keying state across trees MUST include the tree in that key.
<tt>content_ref</tt> resolves only within the <tt>notices/</tt> directory of the tree the
enclosing document was read from. Write authority per tree is distinct and
MUST be structurally enforced, never by configuration the writer itself
reads. <tt>inbound/</tt> is the one tree this document defines; a profile MAY
define others.</t>
        <t><strong>Inbound is Runtime-owned, and read-only is a valid posture.</strong> The
<tt>inbound/</tt> tree is Runtime-owned end to end. A Runtime MAY expose it to the
sandbox read-only — for instance as a read-only mount, which is the
mechanism that actually makes a delivered Notice unforgeable, since file
permission bits do not bind a same-uid Agent, which can change a mode it
owns. A Connector MUST NOT require write access anywhere under <tt>inbound/</tt>:
it MUST start, relay Notices, and resolve bodies when every write, rename,
and directory creation under <tt>inbound/</tt> fails. <tt>inbound/notices/processed/</tt>
is OPTIONAL — a conventional archive location a Connector MAY use only
where a deployment leaves inbound writable; nothing on either side may
depend on its existence or its contents.</t>
        <t><strong>Consumption tracking is Connector-private.</strong> How a Connector records which
Notices it has already relayed to its Agent is Connector-private state,
outside this protocol. Such state lives in the Connector's own storage
outside the Volume, or in Connector-owned paths under <tt>outbound/</tt> not
enumerated in the layout tree — which MUST NOT use the <tt>req-</tt> prefix,
reserved for Submit Requests and their Sidecars, so that a Runtime's scan of
the drop-box never encounters Connector-private state. Either way, the
Runtime neither reads nor relies on it. A seam-visible consumption signal is
deliberately not specified at this document version; <xref target="app-i"/> records the
reasoning.</t>
        <t><strong>Outbound extension space (v3.1.0 DRAFT).</strong> <tt>outbound/ext/&lt;name&gt;/</tt> is a
Connector-owned side channel, one directory per <tt>&lt;name&gt;</tt>, that a Runtime MAY
read by out-of-band agreement. <strong><tt>&lt;name&gt;</tt> is a stable Connector id: the
Connector chooses it once, and it MUST NOT change when the Connector's
repository, package, or deployment is renamed or moved. A Runtime that reads
the directory MUST treat <tt>&lt;name&gt;</tt> as opaque — it is an identifier, never a
name to be parsed, resolved, or matched against a repository or path.</strong> This
document specifies no shape for anything under it; it lies outside the
<tt>req-</tt> prefix the Runtime scans, and nothing security-relevant may be built
on it. Its first use is a Connector's per-Notice <strong>outcome</strong> file, written
by the receiving Agent's own uid and therefore forgeable by construction. An
outcome is a statement by the receiving side about what it did with a
Notice; a party MUST NOT treat an outcome as proof of delivery or of
non-delivery, and the Runtime's own delivery record is authoritative over it
(<xref target="sec-11-15"/>). There is deliberately no inbound extension space: an
inbound tree is a Runtime assertion about origin, and a Connector cannot
know what a vendor-named assertion means.</t>
      </section>
      <section anchor="sec-4-2">
        <name>Deletion rights</name>
        <t>This table names who may delete each artifact. It is <strong>permissive</strong>: a
party exercising a right it already effectively had — a Runtime deleting
from its own Runtime-owned tree, for instance — changes nothing about what
conformant behavior looks like.</t>
        <table>
          <thead>
            <tr>
              <th align="left">Artifact</th>
              <th align="left">Written by</th>
              <th align="left">May be deleted by</th>
              <th align="left">Basis</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td align="left">
                <tt>outbound/req-&lt;id&gt;.json</tt> (pending) and <tt>req-&lt;id&gt;.attachments/</tt></td>
              <td align="left">Connector</td>
              <td align="left">Runtime</td>
              <td align="left">Consumption — the move to <tt>processed/</tt>, or deletion once the Result is written. The Connector MUST NOT delete or rewrite a published request; the JSON file's presence is the commit signal, and un-publishing is unspecified.</td>
            </tr>
            <tr>
              <td align="left">
                <tt>outbound/processed/req-&lt;id&gt;.json</tt></td>
              <td align="left">Runtime</td>
              <td align="left">Runtime at will; Connector after reading the matching Result</td>
              <td align="left">The Runtime's own archive. <strong>Caveat:</strong> <xref target="sec-5-1"/> derives <tt>req_id</tt> from <tt>max(pending, processed, results) + 1</tt>, so a Connector that deletes here (or in <tt>results/</tt>) MUST preserve <tt>req_id</tt> monotonicity by other durable means, such as its own counter. Correspondingly, a Runtime MUST NOT rely on <tt>processed/</tt> contents as durable state (for deduplication, say) — the Connector may delete there, and the Runtime's own record lives outside this protocol.</td>
            </tr>
            <tr>
              <td align="left">
                <tt>outbound/results/&lt;id&gt;.json</tt></td>
              <td align="left">Runtime</td>
              <td align="left">Connector, once read; Runtime by retention</td>
              <td align="left">No signal in this protocol tells the Runtime that a Connector has read a verdict, so Runtime-side deletion SHOULD be age-based and generous: a Result deleted before it is read is a lost verdict.</td>
            </tr>
            <tr>
              <td align="left">
                <tt>inbound/notices/notice-&lt;id&gt;.json</tt>, <tt>inbound/messages/notice-&lt;id&gt;.json</tt>, and their <tt>.attachments/</tt> Sidecar directories</td>
              <td align="left">Runtime</td>
              <td align="left">Runtime only. Where a deployment leaves inbound writable, the Connector MAY move or delete per the optional <tt>processed/</tt> convention, but MUST NOT depend on the ability.</td>
              <td align="left">Runtime retention policy. Absent a seam-visible consumption signal (<xref target="app-i"/>), retention MUST be conservative and age-based. A Connector already tolerates the disappearance of these artifacts: a read against a missing one returns "not resolved" (<xref target="sec-7-3"/>).</td>
            </tr>
            <tr>
              <td align="left">
                <tt>inbound/cursor.json</tt></td>
              <td align="left">Runtime</td>
              <td align="left">Runtime</td>
              <td align="left">Internal state.</td>
            </tr>
            <tr>
              <td align="left">
                <tt>audit/log.jsonl</tt></td>
              <td align="left">Runtime</td>
              <td align="left">Runtime (rotation)</td>
              <td align="left">Never the Connector.</td>
            </tr>
            <tr>
              <td align="left">
                <tt>identity.json</tt></td>
              <td align="left">Runtime or provisioner</td>
              <td align="left">Runtime or provisioner</td>
              <td align="left">Consumers already tolerate its absence (<xref target="sec-9"/>).</td>
            </tr>
          </tbody>
        </table>
      </section>
    </section>
    <section anchor="sec-5">
      <name>Outbound: Submit Request</name>
      <t>An Agent's only outbound action is dropping an inert request file into its
Namespace. The file carries no send capability; the Runtime enforces policy
before anything leaves.</t>
      <t>A Submit Request is written to <tt>&lt;volume-root&gt;/&lt;agent-id&gt;/outbound/req-&lt;id&gt;.json</tt>
as a JSON text <xref target="RFC8259"/>. Its Sidecar bytes, if any, are written first,
into <tt>req-&lt;id&gt;.attachments/</tt>, one blob per Descriptor, named by that
Descriptor's 0-based array index (<xref target="sec-4-1"/>, commit sentinel).</t>
      <figure anchor="ex-submit-request">
        <name>draft/examples/submit-request.json (illustrative — not a fixture)</name>
        <sourcecode type="json" name="submit-request.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{
  "contract_version": "2",
  "req_id": "00000001",
  "agent_id": "agent.research",
  "ts": "2026-07-20T04:10:56Z",
  "in_reply_to": "1721457600000000000-4242",
  "draft": {
    "to": ["owner@example.com"],
    "cc": [],
    "subject": "Run report",
    "body_text": "The run finished; details below.",
    "reply_to_message_id": null,
    "attachments": [
      {
        "filename": "report.pdf",
        "media_type": "application/pdf",
        "size_bytes": 48213,
        "sha256": "970e441b6c4ecd37abf2541e1276c680bdec8481cef9368ff\
    1d010fd6b784b07"
      }
    ]
  }
}
]]></sourcecode>
      </figure>
      <t>The normative shape is <xref target="app-a"/>. The Submit Request envelope is
<strong>closed</strong>: <tt>additionalProperties</tt> is <tt>false</tt> at every level, and a Runtime
MUST reject a request carrying any unrecognized member. <xref target="sec-10-1"/>
explains why this direction is closed while the Runtime-authored artifacts
are open.</t>
      <section anchor="sec-5-1">
        <name>Field semantics and obligations</name>
        <ul spacing="normal">
          <li>
            <t><strong><tt>contract_version</tt></strong> (REQUIRED) is the wire major version, the string
<tt>"2"</tt> at this document version. A Runtime MUST check it before schema
validation and MUST fail closed on an absent, lower, or higher major
(<xref target="sec-10-1"/>).</t>
          </li>
          <li>
            <t><strong><tt>req_id</tt></strong> (REQUIRED) is <strong>monotonic per Namespace</strong> and MUST NOT be
reused across the drop-box's life; derive it from
<tt>max(pending, processed, results) + 1</tt>. Reuse collides message identifiers
and triggers provider-side idempotency deduplication. <tt>req_id</tt> is pinned
to a filename-safe character set by the schema, but it is not the path
authority: the Runtime derives the path identifier from the validated
filename (<xref target="sec-4-1"/>).</t>
          </li>
          <li>
            <t><strong><tt>agent_id</tt></strong> (OPTIONAL), if present, MUST match the Namespace the
request was found in, and a Runtime MUST reject a mismatch. It is never
the attribution source (<xref target="sec-4-1"/>).</t>
          </li>
          <li>
            <t><strong><tt>ts</tt></strong> (OPTIONAL) is the Connector's timestamp for the request. It is
Agent-authored and therefore advisory.</t>
          </li>
          <li>
            <t><strong><tt>in_reply_to</tt></strong> (OPTIONAL) correlates this request with prior inbound
work. It carries either a Runtime-minted <tt>notice_id</tt> or a mail message
identifier <xref target="RFC5322"/>. It is a <strong>hint from an untrusted party</strong> and MUST
NOT by itself widen what the request is permitted to do — the same
posture <xref target="sec-8"/> fixes for <tt>task_id</tt>.</t>
          </li>
          <li>
            <t>The Runtime derives the message identity and the <tt>From</tt>/<tt>Reply-To</tt> of the
composed message itself; identity is Runtime-owned, and the Agent does
<strong>not</strong> choose its sending identity.</t>
          </li>
          <li>
            <t><strong><tt>draft.subject</tt></strong> and <strong><tt>draft.body_text</tt></strong> are REQUIRED and MAY be
empty strings. <tt>body_text</tt> is <strong>plain text</strong>; this document defines no
alternative body representation at this version.</t>
          </li>
          <li>
            <t><strong><tt>draft.reply_to_message_id</tt></strong> (OPTIONAL, nullable) names the message
this draft replies to, as an <xref target="RFC5322"/> message identifier. It is
composed directly into an <tt>In-Reply-To:</tt> header, which is why it carries a
control-character pattern in the schema (below).</t>
          </li>
          <li>
            <t><strong>Recipient policy is a Runtime decision.</strong> The Agent's chosen <tt>draft.to</tt>
and <tt>draft.cc</tt> are <strong>not</strong> inherently trusted. Allowlisting,
task-counterpart binding, and any other recipient rule are evaluated
Runtime-side. A populated allowlist plus a fresh recipient is an
exfiltration surface that the Runtime, not the Connector, must close.</t>
          </li>
          <li>
            <t><strong>Recipient syntax and header-injection discipline.</strong> Each <tt>draft.to</tt> and
<tt>draft.cc</tt> element is exactly one bare addr-spec: no display name,
address list, quoted local part, comment, IP literal, or folding
whitespace. The schema pins the pattern. This profile is deliberately
narrower than <xref target="RFC5322"/>, because those excluded forms are exactly what
makes allowlist evaluation non-mechanical; internationalized (EAI/SMTPUTF8)
addresses are out of scope at this version and may be added later. A
Runtime MUST reject — with a top-level <tt>rejected</tt> Result — a request whose
recipient entries do not parse under this profile, MUST evaluate recipient
policy over exactly the recipient set it will transmit to, and MUST NOT
transmit to any address not present in <tt>draft.to</tt> or <tt>draft.cc</tt>.  </t>
            <t>
Separately, a Runtime MUST NOT allow any Agent-authored string —
<tt>subject</tt>, a recipient entry, an attachment <tt>filename</tt>, an attachment
<tt>media_type</tt>, <tt>draft.reply_to_message_id</tt>, <tt>in_reply_to</tt>, <tt>agent_id</tt>,
<tt>ts</tt>, or <tt>body_text</tt> where it abuts headers — to introduce a header
boundary in the composed message. CR, LF, and NUL MUST be rejected or
stripped before composition <strong>even where schema validation was bypassed
upstream</strong>: the schema-level control-character patterns on <tt>subject</tt>,
<tt>filename</tt>, <tt>media_type</tt>, <tt>reply_to_message_id</tt>, <tt>in_reply_to</tt>,
<tt>agent_id</tt>, and <tt>ts</tt> are the first line of defense, not the only one. Two
of those fields matter for a reason worth stating explicitly, because
narrower schema coverage in an earlier revision of this protocol missed
exactly them: <tt>reply_to_message_id</tt> and <tt>media_type</tt> are composed
<em>directly into headers</em> — <tt>In-Reply-To:</tt> and a MIME part's <tt>Content-Type:</tt>
              <xref target="RFC2045"/> respectively — rather than into a body or a display field.</t>
          </li>
          <li>
            <t><strong><tt>draft.attachments[]</tt></strong> (OPTIONAL) is an array of Descriptors, each
carrying <tt>filename</tt>, <tt>media_type</tt>, <tt>size_bytes</tt>, and <tt>sha256</tt>.
<strong><tt>size_bytes</tt> and <tt>sha256</tt> are REQUIRED on every Descriptor</strong>, computed
by the Connector over the <strong>decoded</strong> bytes it wrote, with <tt>sha256</tt> in
lowercase hexadecimal.  </t>
            <t>
There is <strong>no outbound <tt>content_ref</tt></strong>. The Runtime re-derives each
Sidecar's path from <tt>req-&lt;id&gt;</tt> plus the Descriptor's array index, which is
the sole binding authority; an Agent-supplied path string would be pure
disagreement and attack surface, so it is absent from the schema entirely
and <tt>additionalProperties: false</tt> rejects it. <tt>attachments: []</tt> is valid
and is equivalent to the member being absent; the Sidecar directory MUST
then be absent.  </t>
            <t><tt>sha256</tt> here is an integrity and deduplication aid, <strong>not an outbound
trust boundary</strong>: the Agent controls both the bytes and the hash, so a
mismatch catches corruption, never a hostile Agent. Its value to the
Runtime is that it detects an inconsistent request, not that it
authenticates one.</t>
          </li>
        </ul>
      </section>
    </section>
    <section anchor="sec-6">
      <name>Outbound: Result</name>
      <t>A Result is the Runtime's verdict for one consumed Submit Request, written
to <tt>&lt;volume-root&gt;/&lt;agent-id&gt;/outbound/results/&lt;id&gt;.json</tt>.</t>
      <figure anchor="ex-result">
        <name>draft/examples/result.json (illustrative — not a fixture)</name>
        <sourcecode type="json" name="result.json"><![CDATA[
{
  "contract_version": "2",
  "req_id": "00000001",
  "outcome": "accepted",
  "detail": null,
  "reason_code": "allowlisted",
  "ts": "2026-08-09T19:14:00Z",
  "recipients": ["owner@example.com"],
  "message_id": "<20260809191400.1@example.org>",
  "job_id": null,
  "attestation": {
    "form": "self-stamp",
    "token": "selfstamp.v1.abcdef",
    "claims": {
      "agent": "agent.research",
      "acts_for": "owner@example.com",
      "issued_at": "2026-08-09T19:14:00Z"
    }
  },
  "attachments": [
    { "index": 0, "outcome": "accepted", "detail": null }
  ]
}
]]></sourcecode>
      </figure>
      <t>The normative shape is <xref target="app-b"/>. The Result envelope is <strong>open</strong>: a
consumer MUST tolerate members it does not recognize, at every level
(<xref target="sec-10-1"/>).</t>
      <section anchor="sec-6-1">
        <name>Field semantics and obligations</name>
        <ul spacing="normal">
          <li>
            <t><strong>The Runtime writes exactly one Result per Submit Request it consumes.</strong>
A request the Runtime never consumes gets no Result, and no signal in this
protocol distinguishes "not yet" from "never."</t>
          </li>
          <li>
            <t><strong><tt>outcome</tt></strong> (REQUIRED) is one of <tt>accepted</tt>, <tt>queued_for_human</tt>, or
<tt>rejected</tt>. This is a <strong>closed vocabulary</strong>: it gates which processing
path a document takes, so a consumer cannot safely treat an unrecognized
value as opaque, and introducing a new value is a major change
(<xref target="sec-10-1"/>).</t>
          </li>
          <li>
            <t><strong>Any policy miss — allowlist, rate limit, DLP hold, halt — MUST produce
<tt>queued_for_human</tt>.</strong> Never a silent send; never a silent drop.</t>
          </li>
          <li>
            <t><strong><tt>accepted</tt></strong> means the provider accepted the message for relay.
Downstream delivery or quarantine is out of the Runtime's hands and is
surfaced later via an inbound delivery-status notification, if any.</t>
          </li>
          <li>
            <t><strong><tt>detail</tt></strong> (OPTIONAL, nullable) is human-readable text. It carries no
machine semantics.</t>
          </li>
          <li>
            <t><strong><tt>reason_code</tt></strong> (OPTIONAL) records <em>which policy path</em> authorized or
refused the request; human text stays in <tt>detail</tt>. A Runtime <strong>SHOULD</strong>
set a reason on every Result, successes included. The suggested
vocabulary — <tt>recipient_bound</tt>, <tt>allowlisted</tt>, <tt>no_verified_counterparty</tt>,
<tt>recipient_not_allowlisted</tt>, <tt>dlp_hold</tt>, <tt>no_screener</tt>, <tt>halted</tt>,
<tt>rate_limited</tt>, <tt>unsupported_contract_version</tt> — is <strong>extensible and not
enum-locked in the schema</strong>. Two consumer obligations follow: a consumer
MUST tolerate a <tt>reason_code</tt> it does not recognize, treating it as opaque
and never as an error, and a consumer MUST NOT infer the <tt>outcome</tt> from
it. <tt>outcome</tt> is the sole authoritative verdict; <tt>reason_code</tt> only
explains which path produced it, and a Runtime may add codes at any time
without a protocol change. <tt>unsupported_contract_version</tt> is the code a
Runtime sets on the <tt>rejected</tt> Result it writes for a version-refused
Submit Request (<xref target="sec-10-1"/>).</t>
          </li>
          <li>
            <t><strong><tt>ts</tt></strong> (OPTIONAL) is the Runtime-set, of-record time of the verdict, in
ISO-8601 form.</t>
          </li>
          <li>
            <t><strong><tt>recipients</tt></strong> (OPTIONAL) records the recipient set the Runtime
actually transmitted to.</t>
          </li>
          <li>
            <t><strong><tt>message_id</tt></strong> (OPTIONAL, nullable) is the <xref target="RFC5322"/> message
identifier the Runtime assigned to the composed message.</t>
          </li>
          <li>
            <t><strong><tt>job_id</tt></strong> (OPTIONAL, nullable) is a provider-side job identifier,
opaque to this protocol.</t>
          </li>
          <li>
            <t><strong><tt>attestation</tt></strong> (OPTIONAL) is the identity stamp the Runtime applied to
the outbound message. Its <tt>form</tt> is <tt>self-stamp</tt> or <tt>verifiable</tt>,
distinguishing a stamp a stranger cannot verify from one that can be
verified independently. A Connector <em>surfaces</em> an attestation; it never
verifies one, which is a Runtime concern.</t>
          </li>
          <li>
            <t><strong><tt>attachments[]</tt></strong> (OPTIONAL) carries one diagnostic entry per submitted
attachment: <tt>index</tt>, an <tt>outcome</tt> of <tt>accepted</tt>, <tt>dlp_held</tt>,
<tt>rejected_type</tt>, or <tt>too_large</tt>, and an optional <tt>detail</tt>. <strong><tt>index</tt> MUST
equal the entry's own position in the array</strong> — contiguous, unique, and
covering <tt>0</tt> through <tt>n-1</tt> exactly once. Array position is the binding
authority for attachments in both directions (<xref target="sec-4-1"/>). This
constraint is not expressible in JSON Schema and is enforced by the
conformance gate as a named post-check rather than by the schema
(<xref target="app-h"/>).  </t>
            <t><strong>Two paths, never conflated.</strong> A <em>malformed</em> attachment — a size or
digest mismatch, a missing or extra Sidecar, a duplicate index — is not a
policy decision: it fails the whole request with a top-level <tt>rejected</tt>.
A <em>policy hold</em> — DLP, a disallowed type, a size cap, including a
fail-closed hold when no screener is available — is <tt>queued_for_human</tt>.
Per-attachment outcomes are diagnostic detail under <strong>one</strong> governing
top-level outcome; they never license a partial send.</t>
          </li>
        </ul>
      </section>
    </section>
    <section anchor="sec-7">
      <name>Inbound: Deliver Notice and Inbound Message</name>
      <t>The Runtime writes one Deliver Notice per <strong>delivered</strong> message. Quarantined
or unverified mail produces <strong>no Notice</strong>: it is logged Runtime-side and is
never seen by the Agent. The Notice is a <em>pointer and summary</em>; the body is
fetched on demand from a separate spooled artifact (<xref target="sec-7-3"/>) and is
<strong>untrusted content</strong>.</t>
      <figure anchor="ex-deliver-notice">
        <name>draft/examples/deliver-notice.json (illustrative — not a fixture)</name>
        <sourcecode type="json" name="deliver-notice.json"><![CDATA[
{
  "contract_version": "2",
  "notice_id": "1721457600000000000-4242",
  "ts": "2026-07-20T04:00:00Z",
  "kind": "deliver",
  "message": {
    "id": "42",
    "from": "Alex Doe <owner@example.com>",
    "subject": "status?",
    "preview": "hey, how is the run going",
    "mailbox": "inbox",
    "task_id": "0f1e2d3c4b5a6978",
    "thread_id": "T-9182",
    "in_reply_to": "<20260719120000.7@example.com>",
    "references": ["<20260718090000.3@example.com>"],
    "sender_standing": "known",
    "provenance": "external",
    "verdict": {
      "auth": "dmarc-pass",
      "screen": "clean",
      "entitlement": "task-counterparty"
    },
    "attachments": [
      {
        "filename": "invoice.pdf",
        "media_type": "application/pdf",
        "size_bytes": 12345,
        "disposition": "unscanned"
      }
    ]
  }
}
]]></sourcecode>
      </figure>
      <t>The normative shape is <xref target="app-c"/>. The Deliver Notice envelope is <strong>open</strong>:
a consumer MUST tolerate members it does not recognize, at every level
(<xref target="sec-10-1"/>).</t>
      <section anchor="sec-7-1">
        <name>Field semantics and obligations</name>
        <t><strong>The drop rule.</strong> A Connector relays only <tt>deliver</tt> Notices. It MUST drop —
not surface — a Notice that fails <strong>either</strong> of two independent checks,
treating a failure of either one alone as sufficient grounds to drop:</t>
        <ol spacing="normal" type="1"><li>
            <t><strong>Schema failure.</strong> The Notice fails validation against the
deliver-notice schema (<xref target="app-c"/>) for <strong>any reason other than an
unrecognized member</strong>. That single formulation covers a <tt>kind</tt> other than
<tt>deliver</tt>, a missing required member, an enum violation on a closed
vocabulary, a pattern violation (including a traversal or character-set
violation in <tt>notice_id</tt>, or a shape violation in <tt>content_ref</tt>'s own
pattern), a conditional (<tt>allOf</tt> / <tt>if</tt>-<tt>then</tt>) violation, and a type
violation alike, with no need to enumerate schema-failure categories
separately. <strong>An unrecognized member is never grounds for a drop</strong>
(<xref target="sec-10-1"/>); that is the one carve-out from "fails schema
validation."</t>
          </li>
          <li>
            <t><strong>The <tt>content_ref</tt> index and prefix binding.</strong> A Notice can pass check 1
— every member individually well-shaped — and still carry an attachment
Descriptor whose <tt>content_ref</tt> does not equal
<tt>&lt;notice_id&gt;.attachments/&lt;i&gt;</tt> for its own 0-based array position <tt>i</tt>
(<xref target="sec-7-2"/>): a mismatched trailing index, or a leading <tt>notice_id</tt>
prefix pointing into a <em>different</em> Notice's Sidecar directory. A
Connector MUST run this check independently of schema validation and MUST
drop the Notice on failure exactly as it would a schema failure.</t>
          </li>
        </ol>
        <t><strong>This is not a closure over schema validation.</strong> Check 2 binds two members
of the same document — a Descriptor's <tt>content_ref</tt> against the enclosing
document's own <tt>notice_id</tt> and against the Descriptor's own array position —
against each other, which is not expressible in the JSON Schema vocabulary
these schemas use <xref target="JSON-SCHEMA-2020-12"/>. It therefore cannot be folded
into "fails schema validation" no matter how the schema is phrased: it is a
second, independent condition, not a special case of the first. The
conformance suite enforces it as a named post-check and pins it with a
fixture that is schema-valid on its own and fails only this check
(<xref target="app-h"/>).</t>
        <t>A <strong>version refusal</strong> — an absent or non-matching <tt>contract_version</tt> major —
is explicitly <strong>not</strong> part of this drop path. It is handled first and
separately (<xref target="sec-10-1"/>) as a distinct, logged outcome: a Connector checks
the version before it ever reaches schema validation, so a version-refused
Notice never falls into the drop rule at all.</t>
        <t>A document with <tt>kind: "peer"</tt> found under <tt>inbound/notices/</tt> fails check 1
against this schema, and is dropped the same way (<xref target="app-f"/>, the
peer-origin profile's cross-lock).</t>
        <t><strong>Field obligations.</strong></t>
        <ul spacing="normal">
          <li>
            <t><strong><tt>contract_version</tt></strong>, <strong><tt>notice_id</tt></strong>, <strong><tt>ts</tt></strong>, <strong><tt>kind</tt></strong>, and
<strong><tt>message</tt></strong> are REQUIRED. <tt>notice_id</tt> is Runtime-minted and is pinned by
the schema to a character set that is safe as a path component and as a
<tt>content_ref</tt> prefix.</t>
          </li>
          <li>
            <t>Required members of <tt>message</tt> are <tt>id</tt>, <tt>from</tt>, <tt>subject</tt>, <tt>preview</tt>, and
<tt>mailbox</tt>. The rest is enrichment (<xref target="sec-10-2"/>).</t>
          </li>
          <li>
            <t><strong><tt>message.id</tt></strong> is the provider's message identifier. It is <strong>external
and not character-set-safe</strong>, and MUST NOT be used as a path component
anywhere (<xref target="sec-4-1"/>).</t>
          </li>
          <li>
            <t><strong><tt>message.from</tt> is attacker-controlled display data.</strong> Its display-name
half is chosen freely by the sender — <tt>"security@yourbank.example
&lt;attacker@evil.example&gt;"</tt> is a legal value — and it MUST NOT be used for
any trust decision, nor rendered to a human or to an Agent as verified
identity. Verified sender identity is only ever what the Runtime
summarizes into <tt>sender_standing</tt> and <tt>verdict</tt>; it is never the <tt>from</tt>
string itself.</t>
          </li>
          <li>
            <t><strong>Presentation obligations.</strong> A Connector that presents a message body to
its Agent MUST convey two things, whatever words it uses: quoted or
forwarded material inside the body carries no authority of its own — a
message's authority extends to what its author wrote, never to what they
relayed — and that an attachment's <tt>disposition</tt> is the Runtime's assertion
about handling and not a scanner verdict, so a Connector MUST NOT imply that
bytes have been screened and MUST NOT contradict the per-attachment status
the Runtime published. A Connector MUST also convey that an attachment's
<tt>filename</tt> and <tt>media_type</tt> are sender-chosen display strings on the mail
tree; on a tree whose sender the Runtime authenticated it SHOULD, and MUST
where the attachment was relayed rather than authored by that sender. On the mail
tree a Connector MUST additionally convey that the sender is not
authenticated beyond whatever standing the Runtime has already summarized
— carried in <tt>sender_standing</tt> and <tt>verdict</tt>, named here as the locus and
not as vocabulary to repeat to a reader — and that <tt>message.from</tt> is
sender-chosen text. These are
obligations about substance, not vocabulary: a Connector satisfies them
with any wording its reader can act on without consulting this document,
and they MUST be satisfiable by a literal string the Connector carries,
requiring no lookup or computation when a body is presented.</t>
          </li>
          <li>
            <t><strong>The untrusted-content posture covers every sender-originated string</strong> in
the Notice and in the spooled Inbound Message: <tt>message.from</tt>, <tt>subject</tt>,
<tt>preview</tt>, <tt>body_text</tt>, <tt>references</tt>, attachment <tt>filename</tt> and
<tt>media_type</tt>, and the Inbound Message's <tt>from</tt>, <tt>to</tt>, <tt>cc</tt>, and <tt>subject</tt>.
This is not limited to the members called out individually here.</t>
          </li>
          <li>
            <t><strong><tt>message.sender_standing</tt></strong> (OPTIONAL) is an <strong>open, advisory
vocabulary</strong> — <tt>known</tt>, <tt>agent-tier</tt>, <tt>first-contact</tt>, <tt>read-only</tt>, and
others. How the values are computed is Runtime policy and out of scope;
the wire fixes only today's known vocabulary, so that a Connector can tell
its Agent "stranger," "agent," or "needs sign-off." A Connector MUST
tolerate a value outside this list, treating it as opaque — never as
grounds to drop the Notice — and a Runtime MAY add a value as a minor
change (<xref target="sec-10-1"/>).</t>
          </li>
          <li>
            <t><strong><tt>message.provenance</tt></strong> (OPTIONAL) classifies the message source —
<tt>external</tt>, <tt>internal</tt>, and others — so that a Runtime can route
provenance-labelled screening. It is advisory metadata, like <tt>verdict</tt>,
and never a license for an Agent to obey body content. It is likewise an
<strong>open, advisory vocabulary</strong>: a Connector MUST tolerate a value outside
this list, treating it as opaque, and MUST NOT drop the Notice on its
account; a Runtime MAY add a value as a minor change.</t>
          </li>
          <li>
            <t><strong><tt>message.verdict</tt></strong> (OPTIONAL) carries the upstream authentication,
screening, and entitlement results the Runtime consumed. A Connector
treats it as advisory metadata, never as license for an Agent to obey body
content. The object is open: unrecognized members inside it are tolerated,
as they are everywhere else in this artifact.</t>
          </li>
          <li>
            <t><strong><tt>message.thread_id</tt></strong>, <strong><tt>message.in_reply_to</tt></strong>, and
<strong><tt>message.references</tt></strong> (all OPTIONAL) carry provider and <xref target="RFC5322"/>
threading identity. They grant correlation, never trust (<xref target="sec-8"/>).</t>
          </li>
          <li>
            <t><strong><tt>message.task_id</tt></strong> (OPTIONAL) is Runtime-minted; <xref target="sec-8"/> states the
obligations on it.</t>
          </li>
        </ul>
        <t><strong>Direction and openness.</strong> This artifact is Runtime-authored, and its
envelope is open at every level: a Connector MUST tolerate a member it does
not recognize, in <tt>message</tt> or at the top level, and MUST NOT drop the
Notice on that account. Emitting an unrecognized member remains Runtime
non-conformance (<xref target="sec-10-1"/>).</t>
      </section>
      <section anchor="sec-7-2">
        <name>Inbound attachment Descriptors</name>
        <t><strong><tt>message.attachments[]</tt></strong> (OPTIONAL) is an array of Descriptors carrying
<tt>filename</tt>, <tt>media_type</tt>, and <tt>size_bytes</tt> — all REQUIRED — plus a REQUIRED
<tt>disposition</tt>, and OPTIONAL <tt>sha256</tt> and <tt>content_ref</tt> members gated on it.</t>
        <t><strong><tt>disposition</tt> is the Runtime's assertion, not a scanner verdict.</strong> Its
values are <tt>clean</tt>, <tt>stripped</tt>, <tt>quarantined</tt>, and <tt>unscanned</tt>, and the
vocabulary is <strong>closed</strong>: it gates byte publication, so a new value would be
a behavioral fork a consumer cannot treat as opaque, and introducing one is
a major change (<xref target="sec-10-1"/>). On what basis a Runtime asserts <tt>clean</tt> is
deliberately unspecified — a screening pipeline's pass in one deployment, a
closed operator-supervised topology in another. That is Runtime policy and
out of scope for the wire, the same posture <tt>sender_standing</tt> takes: the
wire fixes the vocabulary, never the computation. The Runtime is trusted by
construction (<xref target="sec-3-1"/>); the load-bearing invariant is that the <em>Agent</em>
is not.</t>
        <t>The counterpart obligation is equally plain: <strong>a Runtime that cannot make
the determination MUST NOT assert <tt>clean</tt>.</strong> It emits <tt>unscanned</tt> — or
<tt>stripped</tt> or <tt>quarantined</tt> where it made that decision — and publishes no
bytes.</t>
        <t><strong>Byte publication and the access grant.</strong> A Runtime MAY publish an
attachment's bytes at <tt>inbound/notices/&lt;notice-id&gt;.attachments/&lt;index&gt;</tt>
          <strong>only</strong> for a Descriptor it asserts <tt>disposition == "clean"</tt>, and only then
MAY it emit <tt>content_ref</tt> (a path of the form
<tt>&lt;notice-id&gt;.attachments/&lt;index&gt;</tt>) and <tt>sha256</tt>. Byte-absence remains the
gate (<xref target="sec-4-1"/>) for anything not asserted clean. Exposing <tt>sha256</tt> on a
withheld item would be a confirmation oracle for no integrity value, which
is why it is clean-gated too. Both conditionals are expressed in the schema.</t>
        <t><strong>The <tt>content_ref</tt> binding.</strong> <tt>content_ref</tt>'s leading <tt>notice_id</tt> prefix
MUST equal the enclosing document's own <tt>notice_id</tt>, and its trailing
<tt>&lt;index&gt;</tt> MUST equal the Descriptor's own 0-based array index. The array
index remains the sole binding authority (<xref target="sec-4-1"/>), and a consumer that
resolves bytes MUST derive the path from the enclosing <tt>notice_id</tt> and the
index — never trusting the <tt>content_ref</tt> string's own text as the path,
since a Descriptor could otherwise point into another Notice's Sidecar
directory. This is a wire-shaped, document-level obligation, enforced by the
conformance gate as a named post-check (<xref target="app-h"/>) in the same way the
Result-side <tt>attachments[i].index == i</tt> binding is.</t>
        <t><strong>A grant, never a directive.</strong> A consumer MUST tolerate a Descriptor whose
bytes it does not resolve — relaying the Notice as usual — and MUST NOT
treat the presence of <tt>content_ref</tt> as a requirement to fetch. A Connector
that never dereferences <tt>content_ref</tt> remains fully conformant. A Connector
MUST resolve published bytes only within its own Namespace's
<tt>inbound/notices/</tt> tree and MUST reject any <tt>content_ref</tt> that does not
match the pinned pattern — the same confinement <xref target="sec-4-1"/>'s read-side
discipline requires generally.</t>
        <t><strong><tt>filename</tt> and <tt>media_type</tt> here are attacker-controlled, untrusted
display strings.</strong> The untrusted-content posture extends to attachment
Descriptors: a quarantined file named to look like an instruction is still
an injection payload even with its bytes withheld. A <tt>clean</tt> assertion
covers the bytes' screening disposition, never the truthfulness of these
strings.</t>
        <t><strong>Inbound <tt>size_bytes</tt> is provider-declared and therefore advisory and
untrusted</strong> — it comes from the provider's structure report, and is not
Runtime-verified the way outbound <tt>size_bytes</tt> is.</t>
        <t><strong>MIME edges.</strong> <tt>filename</tt> is the <xref target="RFC2047"/> / <xref target="RFC2231"/>-<strong>decoded</strong>,
Runtime-sanitized display string. An "attachment" is any part carrying
<tt>Content-Disposition: attachment</tt> <xref target="RFC2183"/>, or any named or non-inline
text part — inline images and <tt>multipart/related</tt> parts count, which is the
conservative reading of <xref target="RFC2045"/> and <xref target="RFC2046"/>. The declared
<tt>media_type</tt> is display-only: the Runtime sniffs the bytes, and the sniffed
type governs policy on the outbound gate.</t>
      </section>
      <section anchor="sec-7-3">
        <name>The Inbound Message (body spool)</name>
        <t>The Runtime spools the delivered body at
<tt>&lt;volume-root&gt;/&lt;agent-id&gt;/inbound/messages/notice-&lt;id&gt;.json</tt>, keyed on the
Runtime-minted notice identifier — never on the provider <tt>message.id</tt>, for
the same confirmed-exploit reason given in <xref target="sec-4-1"/>.</t>
        <figure anchor="ex-inbound-message">
          <name>draft/examples/inbound-message.json (illustrative — not a fixture)</name>
          <sourcecode type="json" name="inbound-message.json"><![CDATA[
{
  "contract_version": "2",
  "notice_id": "1721457600000000000-4242",
  "id": "42",
  "date": "Mon, 20 Jul 2026 04:00:00 +0000",
  "from": "Alex Doe <owner@example.com>",
  "to": "agent@example.org",
  "subject": "status?",
  "body_text": "hey, how is the run going",
  "attachments": [
    {
      "filename": "report.pdf",
      "media_type": "application/pdf",
      "size_bytes": 48213,
      "disposition": "unscanned"
    }
  ]
}
]]></sourcecode>
        </figure>
        <t>The normative shape is <xref target="app-d"/>. Required members are <tt>contract_version</tt>,
<tt>notice_id</tt>, and <tt>body_text</tt>; the rest — <tt>id</tt>, <tt>date</tt>, <tt>from</tt>, <tt>to</tt>, <tt>cc</tt>,
<tt>subject</tt>, and <tt>attachments[]</tt> — is enrichment (<xref target="sec-10-2"/>). Descriptors
here follow exactly the rules of <xref target="sec-7-2"/>, including the <tt>content_ref</tt>
binding and the clean gating. This artifact's envelope is <strong>open</strong>, like the
other Runtime-authored artifacts.</t>
        <t><strong>Resolution.</strong> A Connector's read surface resolves a notice identifier to a
body from this artifact only, never from a provider fetch. The body is
<strong>untrusted content</strong>, the same posture as the rest of <tt>message</tt>.</t>
        <t><strong>Enforcement by absence.</strong> A body the Runtime withholds — because of
screening or quarantine, or because the message was never delivered — is
simply not spooled, and a read against a missing artifact returns "not
resolved." There is no other signal and no fallback fetch. The same "not
resolved" response also covers an artifact the Runtime later removes under
its own retention policy (<xref target="sec-4-2"/>): from the Connector's side, retired
and never-spooled are indistinguishable, and both are already-specified
behavior.</t>
      </section>
    </section>
    <section anchor="sec-8">
      <name>Task Correlation</name>
      <t>The Runtime mints and owns <tt>task_id</tt>; a Connector only reads it, to
correlate a Notice with prior work. The minting and thread-resolution
<em>algorithm</em> is Runtime policy and out of scope. This protocol fixes three
security obligations on it.</t>
      <ul spacing="normal">
        <li>
          <t><strong><tt>task_id</tt> MUST be unguessable and Runtime-minted</strong> — an HMAC over the
thread root, for instance. It MUST NOT be a sequential or otherwise
guessable value, and it MUST NOT be a capability an inbound sender can
assert. <em>(A guessable, sequential <tt>task_id</tt> was a confirmed exploit path
in an early implementation.)</em></t>
        </li>
        <li>
          <t><strong>Matching a <tt>task_id</tt>, by itself, MUST NOT grant a message entry into
that task.</strong> Authoritative correlation is header threading <strong>plus</strong>
per-message verified sender identity. A subaddress or header token
supplied by an inbound sender is a <em>hint</em> only, never authoritative: it is
attacker-controllable, and it is stripped by many gateways.</t>
        </li>
        <li>
          <t><strong>Threading grants correlation, not trust.</strong> A message that threads onto
a known task is still identity-verified per message; it does not inherit
the task's standing.</t>
        </li>
      </ul>
    </section>
    <section anchor="sec-9">
      <name>Binding Record</name>
      <t>The Binding Record is written by the Runtime or provisioner at the
<strong>Volume root</strong> — <tt>&lt;volume-root&gt;/identity.json</tt>, a sibling of
<tt>&lt;agent-id&gt;/</tt>, not inside it — and names which mailbox this Volume is bound
to. Its normative shape is <xref target="app-e"/>.</t>
      <t>The Binding Record names which mailbox a Volume is bound to; it is informational and never consulted for enforcement (Section 9).</t>
      <figure anchor="fx-identity-basic-src">
        <name>fixtures/valid/identity-basic.json</name>
        <sourcecode type="json" name="identity-basic.json"><![CDATA[
{
  "contract_version": "2",
  "agent_id": "agent-research",
  "address": "agent@example.org",
  "provisioned_ts": "2026-07-20T04:10:56Z"
}
]]></sourcecode>
      </figure>
      <section anchor="sec-9-1">
        <name>Field semantics and obligations</name>
        <ul spacing="normal">
          <li>
            <t><strong>Informational, not authoritative.</strong> The Volume is an Agent-writable
filesystem, so a compromised Agent can edit or delete this file at will.
Attribution and policy stay keyed on Runtime-side configuration
(<xref target="sec-4-1"/>). This record grants nothing and is never consulted for
enforcement.</t>
          </li>
          <li>
            <t><strong>Consumers MUST tolerate its absence</strong>, and <strong>MUST treat its contents as
untrusted data</strong>: a malformed, oversized, or hostile file MUST never crash
a reader or otherwise affect protocol behavior.</t>
          </li>
          <li>
            <t>Its jobs are narrow by design: operator sanity — a human confirming which
mailbox a Volume is bound to — a pre-start conflict check for whatever
wrote it, catching for instance one Agent's credentials pointed at another
Agent's Volume, and letting a Connector surface the Agent's own address
back to its Agent.</t>
          </li>
          <li>
            <t><strong>The file is OPTIONAL</strong> (<xref target="sec-10-2"/>): nothing in this protocol
requires a Runtime to write it or a Connector to read it, and both sides
MUST work correctly with it absent.</t>
          </li>
          <li>
            <t>Required members, if the file is present, are <tt>agent_id</tt>, <tt>address</tt>, and
<tt>provisioned_ts</tt>. <tt>label</tt> is OPTIONAL. <tt>contract_version</tt> is OPTIONAL
here — the one artifact where it is — because the file is not a protocol
artifact a consumer must parse to function; if present it is pinned to
<tt>"2"</tt>.</t>
          </li>
          <li>
            <t>This artifact's envelope is <strong>closed</strong> (<tt>additionalProperties: false</tt>),
for a different reason than the Submit Request's is: closure here is an
anti-secret-leak lint, catching a provisioner that writes credential
material into a file living on an Agent-readable Volume. It is not a
compatibility surface.</t>
          </li>
        </ul>
      </section>
    </section>
    <section anchor="sec-directory">
      <name>The Peer Directory</name>
      <t>A Runtime MAY publish, at the Agent's Namespace root
(<tt>&lt;agent-id&gt;/directory.json</tt>, beside the lanes rather than inside one), a
per-instance projection of the authorisation graph naming which peers that
Agent may address.</t>
      <figure anchor="ex-directory">
        <name>draft/examples/directory.json (illustrative — not a fixture)</name>
        <sourcecode type="json" name="directory.json"><![CDATA[
{
  "contract_version": "2",
  "router": "amap.router@fleet.example",
  "written_at": "2026-09-18T12:00:00Z",
  "delegate_to": [
    "bravo@fleet.example",
    "charlie@fleet.example"
  ],
  "mail_to": []
}
]]></sourcecode>
      </figure>
      <ul spacing="normal">
        <li>
          <t><strong>Advisory, never authoritative.</strong> The directory is the Runtime's most recent
word about the graph, and grants nothing. A Connector MUST NOT treat an
address's presence as a prediction that a submission to it will be accepted,
nor its absence as a prediction of refusal: authorisation is evaluated by the
Runtime at submission time, and the Result (<xref target="sec-6"/>) is the only thing that
reports what happened. A consumer that reads the directory as a prediction is
wrong even when the file is current.</t>
        </li>
        <li>
          <t><strong>A Runtime MUST NOT read <tt>directory.json</tt> back, for any purpose.</strong> It is
write-only from the Runtime's side: the graph is derived from policy, the
file is a projection of the graph, and nothing derives the graph from the
file. Enforcement remains the Runtime's own check against its graph, on every
message. The prohibition is on the direction rather than the purpose, because
the ways this decays — as an optimisation, a cache, a recovery source, or a
cross-check — all have innocent intent, and the last two turn a projection
into a source. A projection that can be read back becomes a cache; a cache
becomes a record; a record that disagrees with policy wins by accident.</t>
        </li>
        <li>
          <t><strong>Per-instance projection.</strong> Each Agent's directory carries only the edges
that Agent is party to. It MUST NOT carry the set of peers who may address
that Agent, which is an allowlist in all but name, and MUST NOT carry the
Agent's own address — attribution is the Namespace a file arrived in
(<xref target="sec-4"/>), so an address an Agent reads is a claim rather than an identity.</t>
        </li>
        <li>
          <t><tt>router</tt> is the publishing Runtime's own address and MUST NOT appear in
either list. Every address is one bare addr-spec (<xref target="sec-5"/>).</t>
        </li>
        <li>
          <t><strong>Both lists are always present</strong>, and empty when that lane is closed for this
Agent; <tt>mail_to</tt> is empty in a delegation-only deployment. Absent and empty
MUST NOT be conflated.</t>
        </li>
        <li>
          <t><strong>Written whole and atomically.</strong> A Runtime MUST NOT publish a newly adopted
instance's address to others before it has adopted that instance itself.</t>
        </li>
        <li>
          <t><strong>A Runtime that publishes the directory MUST ensure the Agent can read it</strong>,
and SHOULD expose it read-only. Publishing to a path the Agent's Namespace
does not expose is not conformant: the write succeeds, the file is correct,
and no Agent can read it — a failure that reports nothing on either side.
Publishing is optional, so a Runtime that cannot make the file readable MUST
NOT publish it; an unreadable directory is indistinguishable from an absent
one to a consumer, and worse than an absent one to an operator, who has a
file on disk that looks like it is working. The trap follows from the
location: the file deliberately belongs to no lane, which is what keeps a
delegation-only deployment from materialising a mail tree, and is exactly
what places it outside an exposure built lane by lane.</t>
        </li>
        <li>
          <t><strong><tt>written_at</tt></strong> is the only staleness signal a consumer gets: an honest
marker, not a freshness guarantee.</t>
        </li>
        <li>
          <t><strong>Where the consumer is an Agent rather than a parser, these become
obligations to convey.</strong> A Connector that surfaces the directory to its Agent
instead of interpreting it satisfies the obligations above by stating them,
under the presentation rule in <xref target="sec-7"/>: the Agent is the party forming a
belief, so the Connector MUST convey that an address's presence is not a
prediction of acceptance, that the file may be absent, that an empty list and
an absent list differ, and that <tt>written_at</tt> is the only staleness signal
there is. A Connector with no directory parser does not thereby satisfy them
vacuously.</t>
        </li>
        <li>
          <t><strong>The file MAY be absent</strong> — before first adoption, on a Runtime that does
not implement this section, or after a failed write — and a consumer MUST
tolerate absence exactly as it tolerates a missing Binding Record
(<xref target="sec-9"/>).</t>
        </li>
        <li>
          <t><strong>A Runtime that cannot write it keeps routing.</strong> The directory is advisory,
so a failed write loses a convenience, not the ability to enforce; failing
closed would convert an advisory artifact into a dependency. The Runtime MUST
log the failure. The Agent's only signal remains <tt>written_at</tt> going stale.</t>
        </li>
      </ul>
    </section>
    <section anchor="sec-10">
      <name>Versioning and Conformance</name>
      <section anchor="sec-10-1">
        <name>Versioning</name>
        <t><strong>The fail-closed rule.</strong> <tt>contract_version</tt> is a major-version string. A
consumer that reads a <strong>higher major</strong> than it speaks MUST <strong>fail closed</strong>
— refuse to process the artifact — and MUST NOT silently mis-parse it.</t>
        <t><strong>Version-check ordering.</strong> A consumer MUST implement the version check by
reading <tt>contract_version</tt> <strong>before</strong> any schema validation, comparing
majors as non-negative decimal integers. An absent major, a lower major, or
a higher major is a <strong>version refusal</strong>: fail closed, as a distinct, logged
outcome, never folded into the malformed-document drop path of
<xref target="sec-7-1"/>.</t>
        <t>Checking the version first is what makes fail-closed implementable at all.
A validator that runs schema checks first cannot distinguish "wrong
version" from "malformed," and a strict schema would reject a future-major
document for the wrong reason entirely.</t>
        <t>This ordering rule is about <em>inbound</em> artifacts a Connector consumes, which
have no mechanism in this protocol to carry a distinct outcome back to the
Runtime. It does not reach the <em>outbound</em> direction: a Runtime
version-refusing a Submit Request MAY carry that refusal on the ordinary
request-rejection path — a top-level <tt>rejected</tt> Result with
<tt>reason_code: "unsupported_contract_version"</tt> (<xref target="sec-6-1"/>) — and, per the
conformance classes below, MUST do so for the version-absent request
fixture. That is still a distinct, legible outcome rather than a fold into
the malformed-request case: <tt>reason_code</tt> is exactly this protocol's
mechanism for saying <em>why</em> a <tt>rejected</tt> fired, and a consumer MUST already
tolerate a <tt>reason_code</tt> it does not otherwise interpret. A Runtime has no
equivalent document to write for a version-refused <em>Notice</em> it never emits
— hence the asymmetry: logged-only inbound, <tt>rejected</tt> outbound.</t>
        <t><strong>Why a version signal, rather than unknown-member rejection, is the
boundary.</strong> At the transition from wire major <tt>"1"</tt> to <tt>"2"</tt>, when
attachment support was added, every top-level schema and every nested
object except <tt>verdict</tt> and <tt>attestation.claims</tt> was
<tt>additionalProperties: false</tt>. Under that regime, adding a member is not a
safe additive change the way it would be under a permissive envelope: a
strict major-1 validator rejects the <strong>whole document</strong>, not just the new
member. Inbound, that is <strong>silent total message loss</strong> — an
attachment-bearing email vanishes instead of degrading. Outbound, it is an
accidental schema error rather than a designed refusal. And there is no
signal to gate on while <tt>contract_version</tt> stays <tt>"1"</tt>, because the new
member is invisible to anything that would need to react to it. That is why
attachments required a <strong>major</strong> bump: a major-1 consumer receiving a <tt>"2"</tt>
document hits fail-closed <em>by version</em>, a legible and designed refusal, and
both sides upgrade deliberately.</t>
        <t>Explicitly deleted from this protocol's reasoning: any framing in which
<tt>additionalProperties: false</tt> rejection <strong>is</strong> the version boundary. That
conflates two different axes — schema strictness and protocol version —
and produces catastrophic behavior (whole-document loss) exactly where a
version check should produce a legible refusal.</t>
        <t><strong>Unknown-member tolerance is direction-aware.</strong> This is the rule that
repairs the versioning model, and it is stated per direction because the
two directions have opposite threat models.</t>
        <ul spacing="normal">
          <li>
            <t><strong>Runtime-authored artifacts — Deliver Notice, Inbound Message, Result —
are open.</strong> A consumer MUST ignore members it does not recognize, at
every level of the document, and MUST NOT reject, drop, or fail the
artifact on their account. Strictness on this direction defended nothing:
the producer is trusted by construction, and in the one posture where an
attacker can author a Notice — a writable inbound tree — that attacker can
author a fully schema-valid one, so strictness was never the defense
there. Its failure mode was silent mail loss. Emitting an unknown member
remains a <strong>producer</strong> non-conformance: a Runtime MUST NOT emit members
undefined at the version it claims. In the schemas, <tt>additionalProperties:
false</tt> is absent from the deliver-notice, inbound-message, and result
schemas at every level — top level, <tt>message</tt>, Descriptor items, and
<tt>attestation</tt> — while the clean-gating conditionals are orthogonal and
remain.</t>
          </li>
          <li>
            <t><strong>The Submit Request envelope stays closed</strong>, <tt>additionalProperties:
false</tt> at every level. Closure is doing real security work there: it is
what bars an outbound <tt>content_ref</tt> and any other smuggled member; the
validator is the trusted Runtime; and a refusal is a <em>legible,
per-request</em> <tt>rejected</tt> Result, never a silent loss. A Runtime MUST reject
a request carrying unrecognized members.</t>
          </li>
          <li>
            <t><strong>Consequently:</strong> adding an optional member to a Runtime-authored
artifact is genuinely additive and is a minor change; adding any member
to the Submit Request is a major change.</t>
          </li>
          <li>
            <t>The Binding Record is unchanged on this axis and stays closed for the
separate reason given in <xref target="sec-9-1"/>.</t>
          </li>
        </ul>
        <t><strong>Tolerance extends to values, not only members.</strong> An optional, explicitly
advisory enum on a Runtime-authored artifact — <tt>message.sender_standing</tt> and
<tt>message.provenance</tt> (<xref target="sec-7-1"/>) — is scoped the same way. A consumer
MUST tolerate a value it does not recognize, treating it as opaque or as the
member being absent, and MUST NOT reject, drop, or fail the artifact on
that account; a Runtime MAY add a value to either vocabulary as a minor
change. This closes the same failure mode one indirection over: without it,
a Runtime adding a fifth <tt>sender_standing</tt> or a third <tt>provenance</tt> value
would silently lose every Notice carrying it, against every Connector still
validating the old enum — identical to the member case, just at the value
level. In the schemas, the <tt>enum</tt> constraint is absent from these two
members.</t>
        <t>This is <strong>not</strong> a universal loosening of every enum. <tt>kind</tt>, <tt>outcome</tt> —
both the top-level Result outcome and the per-attachment outcome — and the
inbound Descriptor's <tt>disposition</tt> remain <strong>closed, enum-locked
vocabularies</strong>. <tt>kind</tt> and <tt>outcome</tt> gate which processing path a document
takes at all, and <tt>disposition</tt> gates byte publication (<xref target="sec-7-2"/>), so a
new value in any of them is a behavioral fork a consumer cannot safely treat
as opaque. Introducing one is a major change.</t>
        <t><strong>Two version axes, allowed to diverge.</strong> The <strong>wire major</strong> tracks
whole-document acceptance compatibility. The <strong>document version</strong> tracks
the fuller set of obligations, including narrower, disclosed producer-side
tightenings that do not rise to that level. Document version 3.0.0 is a
major document bump — it moves fixtures between the valid and invalid sets,
and it adds Runtime obligations a strictly-compliant earlier implementation
does not meet — while the wire major stays <tt>"2"</tt>.</t>
        <t>Document version 3.1.0 (DRAFT) is an additive <strong>minor</strong> bump over 3.0.0;
the wire major stays <tt>"2"</tt>. It introduces the outbound extension space and
its one normative clarification (<xref target="sec-4-1"/>, <xref target="sec-11-15"/>), and the
peer-origin profile's schema and conformance fixtures (<xref target="app-f"/>,
<xref target="app-h"/>). No existing schema rule or fixture changed. The profile's
prose is a companion document not yet drafted; until it is, the schema and
the <tt>peer-*</tt> fixtures are normative for the shape of a peer notice, and this
document specifies nothing about the <tt>peer/</tt> tree beyond that shape.</t>
        <t>The reasoning for holding the wire major is worth stating, because the
alternative is tempting and wrong. The wire major exists to force a legible
fail-closed refusal when a <em>whole document</em> that a counterpart already
accepts would otherwise be silently misread or silently dropped — the
catastrophic, undetectable failure mode a version bump exists to convert
into a designed refusal. Document version 3.0.0 does <strong>not</strong> claim that
producer bytes are unchanged; they are not, on both sides (see the change
log). But neither narrowing is that failure mode: each travels a designed,
legible, per-artifact refusal path this protocol has always sanctioned,
rather than a whole-document rejection triggered by an unrelated member.
The Submit Request tightening yields a per-request <tt>rejected</tt> Result; the
<tt>notice_id</tt> character-set tightening yields a per-Notice Connector-side
drop under check 1 of <xref target="sec-7-1"/> — a real cost, disclosed in the change
log, but a narrow and legible one, not silent total loss. Bumping the wire
major to <tt>"3"</tt> would instead force every major-2 counterpart on <strong>both</strong>
sides to refuse <strong>all</strong> traffic outright, including all the traffic the
tolerance rule newly protects — inflicting exactly the catastrophic,
undisclosed mail loss this version exists to repair, in order to fix
incompatibilities that already have narrower, disclosed failure paths.</t>
        <t><strong>Corrections.</strong> Two claims that shipped in earlier revisions of this
protocol's normative text were false, and are named here rather than
quietly replaced, because an implementer who read them may have built
against them.</t>
        <ol spacing="normal" type="1"><li>
            <t><strong>The write-rule "strengthening" that was a regression.</strong> An earlier
revision described the exclusive-create and no-symlink-follow
requirement on the final path component as having been "strengthened
to" the pinned directory-descriptor walk — that is, replaced by it — and
deleted the earlier clause outright. That was a <strong>weakening</strong>, not a
strengthening: a parent chain pinned by descriptor is silent about the
final <tt>.tmp</tt> filename itself, so dropping exclusive-create there let an
Agent that pre-plants a symlink at a predictable <tt>.tmp</tt> name have the
Runtime write through it. <xref target="sec-4-1"/> now states the rule
conjunctively, with all three legs required together.</t>
          </li>
          <li>
            <t><strong>The drop rule described as a closure over schema validation.</strong> An
earlier revision described the inbound drop rule as a pure closure over
schema validation, and claimed that formulation was exact against the
fixture set. Both statements were false: the <tt>content_ref</tt> index and
prefix binding is not expressible in the schema dialect used here, and a
fixture exists that is schema-valid on its own and must nonetheless be
dropped. <xref target="sec-7-1"/> now states two independent checks, and so does the
Connector conformance class below.</t>
          </li>
        </ol>
        <t>A third correction concerns this protocol's own change history rather than
an obligation, and is recorded in the change log: the document version that
introduced <tt>message.provenance</tt> was labelled an additive minor change,
which was unsound against the strictly-validating consumers of the time.
The change log states what actually happened rather than relabelling the
history.</t>
        <t><strong>Change control.</strong> A capability change starts with a change to this
document, its schemas, and its fixtures, <em>before</em> either side implements
it. Neither a Runtime nor a Connector may invent a wire member locally.
Additive and backward-compatible changes are minor bumps; any change that
breaks an existing fixture is a major bump, taken deliberately by both
sides.</t>
      </section>
      <section anchor="sec-10-2">
        <name>Required versus optional</name>
        <t><tt>contract_version</tt> is REQUIRED on every protocol artifact — Submit Request,
Deliver Notice, and Result — with the Binding Record as the single
exception (<xref target="sec-9-1"/>). The rest of this table reflects what
implementations emit today; specified-but-unbuilt enrichment stays
optional, and tightening any of it to required would be a future major
bump.</t>
        <table>
          <thead>
            <tr>
              <th align="left">Artifact</th>
              <th align="left">Required</th>
              <th align="left">Optional (specified; may be absent)</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td align="left">
                <strong>deliver-notice</strong></td>
              <td align="left">
                <tt>contract_version, notice_id, ts, kind, message{id, from, subject, preview, mailbox}</tt></td>
              <td align="left">
                <tt>message.{task_id, thread_id, in_reply_to, references, sender_standing, verdict, provenance}</tt>, <tt>message.attachments[]</tt> (items require <tt>filename, media_type, size_bytes, disposition</tt>; <tt>sha256</tt> and <tt>content_ref</tt> optional, gated to <tt>disposition == "clean"</tt>)</td>
            </tr>
            <tr>
              <td align="left">
                <strong>submit-request</strong></td>
              <td align="left">
                <tt>contract_version, req_id, draft{to, subject, body_text}</tt></td>
              <td align="left">
                <tt>agent_id, ts, in_reply_to, draft.{cc, reply_to_message_id}</tt>, <tt>draft.attachments[]</tt> (items require <tt>filename, media_type, size_bytes, sha256</tt>)</td>
            </tr>
            <tr>
              <td align="left">
                <strong>result</strong></td>
              <td align="left">
                <tt>contract_version, req_id, outcome</tt></td>
              <td align="left">
                <tt>detail, reason_code, ts, recipients, message_id, job_id, attestation</tt> (requires <tt>form</tt>; <tt>token</tt> and <tt>claims</tt> optional), <tt>attachments[]</tt> (items require <tt>index, outcome</tt>)</td>
            </tr>
            <tr>
              <td align="left">
                <strong>inbound-message</strong></td>
              <td align="left">
                <em>(the artifact itself is OPTIONAL — <xref target="sec-7-3"/>)</em>; if present: <tt>contract_version, notice_id, body_text</tt></td>
              <td align="left">
                <tt>id, date, from, to, cc, subject</tt>, <tt>attachments[]</tt> (same Descriptor rules as deliver-notice)</td>
            </tr>
            <tr>
              <td align="left">
                <strong>binding-record</strong></td>
              <td align="left">
                <em>(the file itself is OPTIONAL — <xref target="sec-9"/>)</em>; if present: <tt>agent_id, address, provisioned_ts</tt></td>
              <td align="left">
                <tt>label</tt>; <tt>contract_version</tt> (optional — if present, pinned to <tt>"2"</tt>)</td>
            </tr>
          </tbody>
        </table>
        <t><tt>inbound/notices/processed/</tt> is an OPTIONAL <strong>directory convention</strong>
(<xref target="sec-4-1"/>), not a protocol artifact: it carries no schema, and no
fixture exists or is needed for it.</t>
        <t>Emission of <tt>contract_version</tt> on the three protocol artifacts is
mandatory. An implementation not yet emitting it has an implementation
conformance gap, not a license from this document.</t>
      </section>
      <section anchor="sec-10-3">
        <name>Conformance</name>
        <t><strong>The fixtures are the other side.</strong> <tt>fixtures/</tt> holds golden artifacts,
and a producer or consumer proves conformance by passing them with <strong>no
counterpart present</strong> (<xref target="app-h"/>). That is what makes independent
implementation practical: neither side needs the other to exist in order to
demonstrate conformance.</t>
        <t>Not every obligation in this document is wire-shaped. A <em>behavioral</em>
obligation — one about how an implementation acts against a filesystem,
rather than about the shape of a document — lies outside the fixture gate
by construction and is checked operationally instead. <xref target="app-h"/> lists
every obligation of this kind that a green run does not prove.</t>
        <t>This document defines two conformance classes. An implementation MAY claim
both. Claiming either without the corresponding fixture run, and without
the prescribed operational check for that class's behavioral obligations,
is not conformance.</t>
        <t><strong>Runtime conformance class.</strong> An implementation claiming Runtime
conformance:</t>
        <ul spacing="normal">
          <li>
            <t>MUST produce Deliver Notice documents valid against the deliver-notice
schema (<xref target="app-c"/>) and Result documents valid against the result schema
(<xref target="app-b"/>); MAY produce Inbound Message documents, which when produced
MUST be valid against the inbound-message schema (<xref target="app-d"/>); and MAY
produce a Binding Record valid against the binding-record schema
(<xref target="app-e"/>).</t>
          </li>
          <li>
            <t>MUST consume Submit Request documents: it MUST accept every valid
<tt>request-*</tt> fixture and MUST reject every invalid <tt>request-*</tt> fixture.</t>
          </li>
          <li>
            <t>MUST demonstrate its producing side by confirming that its own emitted
Notices, Results, and Inbound Messages satisfy the same schemas the
suite's valid <tt>notice-*</tt>, <tt>result-*</tt>, and <tt>message-*</tt> fixtures satisfy —
<strong>including the three fixtures carrying unrecognized members</strong>. A Runtime
MUST NOT itself emit unrecognized members, but its <em>parser</em>, exercised by
those fixtures, is what a Connector conformance run depends on it never
having to special-case. It MUST also confirm that any Binding Record it
writes satisfies the valid <tt>identity-*</tt> shape.</t>
          </li>
          <li>
            <t>MUST uphold the filesystem and policy obligations of Sections <xref format="counter" target="sec-4"/>
through <xref format="counter" target="sec-8"/> that no document validator can observe. <xref target="app-h"/>
lists them explicitly as what a green fixture run does <strong>not</strong> prove, and
they are obligations of the class regardless — write-side path discipline
(the conjunctive parent-chain-pinning, no-follow-while-building, and
non-following-final-component rule), ingest by copy, read-side open
discipline, and single-writer/single-drainer chief among them.</t>
          </li>
        </ul>
        <t><strong>Connector conformance class.</strong> An implementation claiming Connector
conformance:</t>
        <ul spacing="normal">
          <li>
            <t>MUST produce Submit Request documents valid against the submit-request
schema (<xref target="app-a"/>), and MUST NOT emit an outbound <tt>content_ref</tt>
(<xref target="sec-5-1"/>).</t>
          </li>
          <li>
            <t>MUST consume Deliver Notice, Inbound Message, and Result documents: it
MUST accept every valid <tt>notice-*</tt>, <tt>message-*</tt>, and <tt>result-*</tt> fixture —
<strong>including those carrying unrecognized members or unrecognized
advisory-enum values</strong> — and MUST reject, meaning drop rather than
surface, every invalid <tt>notice-*</tt> fixture <strong>except the version-refusal
one</strong>, which it MUST instead refuse via the distinct version-refusal path
(<xref target="sec-10-1"/>): never folded into the drop path, and never surfaced to
the Agent either.  </t>
            <t><xref target="sec-7-1"/>'s drop rule is <strong>two independent conditions, not a closure
over schema validation</strong>. "Fails schema validation for any reason other
than an unrecognized member" (check 1) covers ten of the eleven other
invalid <tt>notice-*</tt> fixtures — missing-required-member, wrong-<tt>kind</tt>,
enum, pattern, and conditional violations alike. The eleventh is
schema-valid on its own, producing zero errors from the gate's schema
validator, and is caught only by the <tt>content_ref</tt> index and prefix
binding (check 2, <xref target="sec-7-2"/>), a named post-check the schema cannot
express. A Connector conformance claim requires running <strong>both</strong> checks;
schema validation alone does not close this set.  </t>
            <t>
Accepting the clean-Descriptor fixture — a Descriptor asserted <tt>clean</tt>
and carrying <tt>content_ref</tt> and <tt>sha256</tt> — is part of this obligation, and
acceptance means relaying the Notice, not fetching the bytes: a Connector
that never dereferences <tt>content_ref</tt> satisfies it (<xref target="sec-7-2"/>).</t>
          </li>
          <li>
            <t>MUST return "not resolved" for a body whose Inbound Message is absent,
and MUST NOT attempt any fallback fetch (<xref target="sec-7-3"/>).</t>
          </li>
          <li>
            <t>MUST tolerate a Binding Record that is absent, and MUST NOT crash or
otherwise misbehave on one that is hostile or malformed (<xref target="sec-9-1"/>).</t>
          </li>
          <li>
            <t>MUST NOT require write access anywhere under <tt>inbound/</tt>, demonstrated by
the operational check below rather than by a fixture (<xref target="sec-4-1"/>).</t>
          </li>
          <li>
            <t>MUST demonstrate its producing side by confirming that its own emitted
requests satisfy the same schema the suite's valid <tt>request-*</tt> fixtures
satisfy, and MUST uphold the write-ordering, atomicity, and
read-discipline obligations of <xref target="sec-4-1"/>, which the fixtures cannot
check.</t>
          </li>
        </ul>
        <t><strong>Read-only inbound tolerance — the prescribed operational check.</strong> Run
the Connector against an inbound tree it cannot write — a genuinely
read-only mount, not merely cleared permission bits, since a same-uid Agent
can change a mode it owns — and confirm that it starts, relays every
pending Notice, resolves bodies, and submits outbound requests, without
requiring any write under <tt>inbound/</tt> to succeed.</t>
      </section>
    </section>
    <section anchor="sec-11">
      <name>Security Considerations</name>
      <t>This section gathers the threats this protocol's obligations exist to
address. It introduces no new mechanism: every requirement referenced here
is stated normatively in Sections <xref format="counter" target="sec-4"/> through <xref format="counter" target="sec-10"/>. The
organizing principle throughout is the invariant of <xref target="sec-3-2"/> —
enforcement is Runtime-side, and no property depends on the Agent or its
Connector behaving.</t>
      <t>The threat actor assumed throughout is a <strong>fully compromised Agent</strong> with
write access to its own Namespace, running as a uid that owns the files it
creates, able to act concurrently with the Runtime, and able to observe or
predict the filenames the Runtime is about to use.</t>
      <section anchor="sec-11-1">
        <name>Path authority and traversal</name>
        <t>An identifier drawn from a file's <em>contents</em> MUST NOT name a write path
(<xref target="sec-4-1"/>). An early implementation trusted a <tt>req_id</tt> member inside a
request body to name the path its Result would be written to, which is a
confirmed exploit: the Agent controls that member and could therefore
direct a Runtime-privileged write anywhere the Runtime could reach. The
obligation is that the Runtime derives identifiers from the <em>validated
filename</em>, over a restricted character set, and re-derives every path from
data it controls.</t>
        <t>The same reasoning is why inbound Sidecar directories and the body spool
are keyed on the Runtime-minted notice identifier rather than the provider
<tt>message.id</tt>: the provider identifier is external, arbitrary, and not
character-set-safe, and using one as a path component was itself a
confirmed exploit path. The schemas pin <tt>notice_id</tt> and <tt>req_id</tt> to
character sets that are safe as path components; <tt>message.id</tt> is
deliberately unconstrained, because it is never used as one.</t>
      </section>
      <section anchor="sec-11-2">
        <name>Symlinks, descriptors, and open discipline</name>
        <t>A shared filesystem between a trusted and an untrusted party makes every
path-based check racy. The Agent can replace any component of a path it
owns with a symlink, at any moment, including between a Runtime's check and
the Runtime's subsequent act.</t>
        <t><xref target="sec-4-1"/> states the write-side discipline as three conjunctive
requirements, and the conjunction is the point: each leg closes a gap the
other two leave open.</t>
        <ul spacing="normal">
          <li>
            <t>Pinning only the final component leaves the <em>parent</em> swappable, and
rename and directory-creation calls have no no-follow flag at all — so a
bare "do not follow symlinks on writes" rule is not sufficient.</t>
          </li>
          <li>
            <t>Pinning the parent chain without refusing symlinks <em>while acquiring</em> it
means the pinned descriptor may point at whatever the Agent planted
before the Runtime opened it — including across a Runtime restart, when
the chain is re-opened.</t>
          </li>
          <li>
            <t>Pinning the whole chain correctly still says nothing about the final
filename. Every Runtime-written filename in the layout tree is
predictable or observable to the Agent, so a pre-planted symlink at that
name receives the Runtime's write with the Runtime's privileges unless
the final open refuses to follow it and, where the file is newly created,
refuses to reuse an existing name at all (<tt>O_EXCL</tt>).</t>
          </li>
        </ul>
        <t>The append-only audit log is the one write with no <tt>.tmp</tt> sibling to lean
on, and it is called out separately for two reasons. <tt>O_EXCL</tt> cannot apply
to a file that legitimately already exists, so the final-component
protection there is a no-follow, append-mode, <strong>non-blocking</strong> open plus an
<tt>fstat</tt> through the resulting descriptor. The non-blocking part is
load-bearing rather than a performance nicety: a no-follow flag does not
apply to a FIFO, because a FIFO is not a symlink, and opening a FIFO for
writing blocks until a reader appears. An Agent that plants a FIFO at that
filename therefore stalls the Runtime's drain indefinitely — a denial of
service that the exclusive-create branch happens to refuse for free, and
that this branch must refuse explicitly.</t>
        <t>The read side mirrors all of it (<xref target="sec-4-1"/>): per-component no-follow
opens or an equivalently pinned walk, a non-blocking open, an <tt>fstat</tt>
through the open descriptor rather than a pre-open <tt>stat</tt> (which is itself
a race), refusal of a multiply-linked file, and enforced size and count
caps. The caps are not optional hardening: the <tt>too_large</tt> Result value
presumes one, and without them an Agent can present an unbounded Sidecar or
an unbounded request and exhaust the Runtime.</t>
      </section>
      <section anchor="sec-11-3">
        <name>Time-of-check to time-of-use, and ingest by copy</name>
        <t>Even with every open performed correctly, validating a file <em>in place</em> and
then using it is a race: the Agent can rewrite the bytes between the check
and the use, so the document that passed policy is not necessarily the
document that gets composed and sent. The obligation that closes this is
ingest by copy (<xref target="sec-4-1"/>) — the Runtime copies the request and every
Sidecar into Runtime-private Staging outside the Volume, and validates,
evaluates policy on, and composes exclusively from that private copy, never
re-reading an Agent-writable path after a decision that depends on its
content.</t>
        <t>This is also what keeps the trust model honest. Without it, the property
"the request the Runtime approved is the request the Runtime sent" would
rest on the Connector not rewriting a published file — an Agent-side
obligation carrying a Runtime-side safety property, which the invariant of
<xref target="sec-3-2"/> forbids. With it, that Connector obligation is pure
well-formedness: a Runtime MAY treat an observed post-publish change as
malformed, but nothing depends on the Connector honoring it.</t>
      </section>
      <section anchor="sec-11-4">
        <name>Concurrency</name>
        <t>Every atomicity guarantee in this protocol — the <tt>.tmp</tt>-then-rename
discipline, the commit sentinel, the derivation of <tt>req_id</tt> from the
maximum of the pending, processed, and result sets — assumes a single
writer and a single drainer per Namespace, and none of them provides
mutual exclusion (<xref target="sec-4-1"/>). Two concurrent drains can deliver the same
request twice; two concurrent writers can collide on a <tt>req_id</tt> and clobber
a published request, producing a verdict that names a request that no
longer exists as written. The exclusion mechanism lives outside this
protocol, and an implementation that does not provide one is not
conformant, because behavior under violation is unspecified rather than
merely degraded.</t>
      </section>
      <section anchor="sec-11-5">
        <name>Attribution spoofing</name>
        <t>Attribution is the Namespace a file arrived in, never an identifier inside
the file (<xref target="sec-4-1"/>). A compromised Agent can write any <tt>agent_id</tt> it
likes into a request; the member exists only as a cross-check, and a
Runtime MUST reject a mismatch rather than believe it. Structural isolation
is what actually enforces this: a Connector scoped to its own Namespace
cannot name another Agent's.</t>
        <t>The Binding Record deserves the same skepticism from the other direction
(<xref target="sec-9-1"/>). It sits on an Agent-writable Volume, so its contents are
untrusted data, it grants nothing, and it is never consulted for
enforcement.</t>
      </section>
      <section anchor="sec-11-6">
        <name>Untrusted content</name>
        <t>Inbound mail is an injection vector aimed at the Agent's reasoning, and
this protocol's posture is that <em>every sender-originated string</em> is
untrusted content: bodies, subjects, previews, threading references, sender
display names, and attachment Descriptors alike (<xref target="sec-7-1"/>).</t>
        <t>Two cases are worth restating because they are the ones most often assumed
safe.</t>
        <t><strong><tt>message.from</tt> is attacker-controlled display data.</strong> Its display-name
half is chosen freely by the sender, so a value such as
<tt>"security@yourbank.example &lt;attacker@evil.example&gt;"</tt> is entirely legal and
renders convincingly. It MUST NOT be used for a trust decision or shown to
a human or an Agent as verified identity. Verified sender identity is only
ever what the Runtime summarizes into <tt>sender_standing</tt> and <tt>verdict</tt>.</t>
        <t><strong>An attachment <tt>filename</tt> is an injection payload.</strong> A Descriptor is
metadata about a file the Agent may never receive the bytes of, and a
<tt>clean</tt> Disposition asserts something about <em>bytes</em>, never about the
truthfulness of the strings describing them. A quarantined file named to
look like an instruction reaches the Agent's context intact even though its
bytes were withheld. <tt>media_type</tt> is likewise a declaration, which is why
policy is governed by the Runtime's own sniffing of the bytes rather than
by the declared type.</t>
        <t><tt>verdict</tt>, <tt>sender_standing</tt>, and <tt>provenance</tt> are advisory metadata for
routing and presentation. None of them is a license for an Agent to obey
body content, and this document deliberately does not define what an Agent
should do with a message — that is Connector and deployment policy.</t>
      </section>
      <section anchor="sec-11-7">
        <name>Recipient policy and header injection</name>
        <t>Recipient policy is the exfiltration boundary, and it is a Runtime decision
(<xref target="sec-5-1"/>). The Agent's <tt>draft.to</tt> and <tt>draft.cc</tt> are requests, not
instructions. Two obligations make the boundary mechanical rather than
best-effort: the Runtime MUST evaluate policy over exactly the recipient
set it will transmit to, and it MUST NOT transmit to any address not
present in those members. Without the first, policy can be evaluated over a
parse of the recipients that differs from the one used for transmission;
without the second, a Runtime-side addition (a bcc, an archive address)
escapes the evaluated set.</t>
        <t>The narrow addr-spec profile exists to make the evaluation mechanical.
Display names, address lists inside a single element, quoted local parts,
comments, IP literals, and folding whitespace are precisely the forms that
make "does this address match the allowlist" a parsing problem with
divergent answers — the classic confusion in which one parser sees the
allowlisted address and the transmitting parser sees the attacker's. The
profile is deliberately narrower than <xref target="RFC5322"/> for that reason, and the
cost is disclosed: some legitimate <xref target="RFC5322"/> addresses cannot be
expressed at this version.</t>
        <t>Header injection is the second half. Any Agent-authored string that reaches
the composed message can attempt to introduce a header boundary with CR,
LF, or NUL. The schema patterns are the first line of defense, but a
Runtime MUST also reject or strip those characters at composition time,
because a document can reach composition by a path where schema validation
was bypassed upstream. The two fields most easily overlooked are the ones
composed <em>directly into headers</em> rather than into a body or display
context: <tt>reply_to_message_id</tt>, which becomes an <tt>In-Reply-To:</tt> header, and
an attachment's <tt>media_type</tt>, which becomes a MIME part's <tt>Content-Type:</tt>
          <xref target="RFC2045"/>. An earlier revision's schema coverage stopped at <tt>subject</tt> and
<tt>filename</tt> and missed exactly these.</t>
      </section>
      <section anchor="sec-11-8">
        <name>The outbound digest is not a trust boundary</name>
        <t><tt>sha256</tt> on an outbound attachment Descriptor is computed by the Connector
over bytes the Connector wrote (<xref target="sec-5-1"/>). The Agent controls both, so
a matching digest proves only that the request is internally consistent.
It catches corruption and interrupted writes; it authenticates nothing. A
Runtime MUST NOT treat a digest match as evidence about the content's
safety, and MUST apply its own policy — sniffing the bytes, applying type
and size rules — to the staged copy regardless.</t>
        <t>The inbound direction is weaker still and is labelled as such: inbound
<tt>size_bytes</tt> is provider-declared and advisory, not Runtime-verified
(<xref target="sec-7-2"/>).</t>
      </section>
      <section anchor="sec-11-9">
        <name>Confirmation-oracle avoidance</name>
        <t>Inbound metadata is gated so that withholding bytes does not become an
information channel. <tt>sha256</tt> on an inbound Descriptor is emitted only
where the Runtime asserts <tt>clean</tt> (<xref target="sec-7-2"/>). Publishing a digest for a
withheld attachment would let an Agent confirm whether a <em>guessed</em> file was
the one quarantined — a confirmation oracle — while providing no integrity
value at all, since the Agent has no bytes to check it against.</t>
        <t>The same principle governs quarantined mail generally: it produces no
Notice at all, rather than a Notice announcing that something was
withheld.</t>
      </section>
      <section anchor="sec-11-10">
        <name>Byte-absence and artifact-absence as the access gate</name>
        <t>The access gate in this protocol is the absence of bytes, not a flag
inside a document (<xref target="sec-4-1"/>). A Runtime MUST NOT place non-clean bytes
in an Agent's Namespace; <tt>content_ref</tt> is the grant, and it exists only
where bytes were published. An Agent that ignores every Disposition it is
shown still cannot read what was never written.</t>
        <t>The same shape appears for bodies: a withheld or never-delivered message is
simply not spooled, and a read against a missing artifact returns "not
resolved," with no fallback fetch available to the Connector
(<xref target="sec-7-3"/>). Because retention may also remove an artifact
(<xref target="sec-4-2"/>), "retired" and "never spooled" are indistinguishable from
the Connector's side — which is a deliberate property, not an ambiguity to
be resolved by a new signal.</t>
        <t>The <tt>content_ref</tt> binding rules (<xref target="sec-7-2"/>) close the corresponding
path-confusion risk: a consumer derives the byte path from the enclosing
<tt>notice_id</tt> and the array index rather than from the <tt>content_ref</tt> string,
and refuses any reference that does not match the pinned pattern or points
outside its own inbound tree. Otherwise a Descriptor could name another
Notice's Sidecar directory — including one belonging to a different
delivery whose bytes the Agent was never granted.</t>
      </section>
      <section anchor="sec-11-11">
        <name>Fail-closed version handling, and the cost of tolerance</name>
        <t>A consumer reading a higher major than it speaks fails closed, and the
version check runs <em>before</em> schema validation so that "wrong version" and
"malformed" remain distinguishable (<xref target="sec-10-1"/>). Folding the two
together is how a protocol acquires silent, undiagnosable mail loss.</t>
        <t>The unknown-member tolerance rule is direction-aware precisely because
tolerance has a security cost in one direction and a safety benefit in the
other. On Runtime-authored artifacts, strictness defended nothing — the
producer is trusted, and an attacker able to author a Notice at all (a
writable inbound tree) can author a schema-valid one — while its failure
mode was silent mail loss. On the Agent-authored Submit Request, closure is
doing real work: it is what makes an outbound <tt>content_ref</tt>, or any other
smuggled member, a hard rejection rather than a member the Runtime might
one day interpret. A Runtime therefore MUST keep rejecting unrecognized
members inbound from an Agent, and its rejection is legible — a <tt>rejected</tt>
Result — rather than silent.</t>
        <t>The value-level tolerance is scoped to the two explicitly advisory
vocabularies for the same reason: <tt>kind</tt>, <tt>outcome</tt>, and <tt>disposition</tt> gate
processing paths and byte publication, and treating an unrecognized value
in any of them as opaque would let a producer's new value silently select a
consumer's default path.</t>
      </section>
      <section anchor="sec-11-12">
        <name>Inbound integrity and the writability of the inbound tree</name>
        <t>A read-only inbound mount is the mechanism that makes a delivered Notice
unforgeable (<xref target="sec-4-1"/>). Permission bits alone do not achieve it: a
same-uid Agent can change a mode it owns, so an inbound tree protected only
by bits is writable by the attacker in practice. Deployments that leave
inbound writable should understand what they are accepting — an Agent can
then author a schema-valid Notice for mail that never arrived, or alter one
that did.</t>
        <t>This is also why the tolerance rule does not weaken the inbound direction:
in the writable-inbound posture, the attacker's forgery is schema-valid
anyway, so schema strictness was never the control. The control is the
mount.</t>
        <t>Correspondingly, a Connector MUST NOT require write access under
<tt>inbound/</tt>, and consumption tracking is Connector-private state kept
elsewhere (<xref target="sec-4-1"/>). A Connector that crashes on a read-only inbound
tree is broken against any Runtime enforcing inbound integrity.</t>
      </section>
      <section anchor="sec-11-13">
        <name>Retention, deletion, and lost verdicts</name>
        <t>Deletion rights are named per artifact class (<xref target="sec-4-2"/>) because an
unnamed right becomes either unbounded growth or an accidental data-loss
path. Two hazards are worth restating.</t>
        <t><strong>A Result deleted before it is read is a lost verdict.</strong> No signal in
this protocol tells a Runtime that a Connector has read one, so
Runtime-side retention SHOULD be age-based and generous.</t>
        <t><strong>Deletion interacts with <tt>req_id</tt> monotonicity.</strong> A Connector exercising
its deletion rights in <tt>processed/</tt> or <tt>results/</tt> removes exactly the
state from which the next <tt>req_id</tt> is derived, and MUST preserve
monotonicity by other durable means. Symmetrically, a Runtime MUST NOT
treat <tt>processed/</tt> as durable state — for deduplication, for instance —
because the Connector may delete there. The Runtime's own record of what
it sent belongs outside this protocol entirely.</t>
      </section>
      <section anchor="sec-11-14">
        <name>What is wire-verifiable, and what is a Runtime obligation</name>
        <t>A green conformance run proves that documents have the right shape. It
proves nothing about a filesystem. The following are Runtime or Connector
obligations that no document validator can observe, and a reviewer
assessing an implementation must check them another way (<xref target="app-h"/> lists
them as the gate's own honesty statement):</t>
        <ul spacing="normal">
          <li>
            <t>Sidecar file existence, and the binding between an ordinal and a
directory entry on disk.</t>
          </li>
          <li>
            <t>Whether <tt>size_bytes</tt> and <tt>sha256</tt> match the bytes actually on disk.</t>
          </li>
          <li>
            <t>Display sanitization of <tt>filename</tt>, and its never-a-path invariant.</t>
          </li>
          <li>
            <t>The keying of inbound Sidecar directories on the Runtime-minted notice
identifier.</t>
          </li>
          <li>
            <t>The location of Runtime-private Staging, outside the mounted Volume.</t>
          </li>
          <li>
            <t>Write ordering and commit-sentinel atomicity.</t>
          </li>
          <li>
            <t>Caps, allowlists, and quotas.</t>
          </li>
          <li>
            <t>The absence of a spooled body for a quarantined or blocked message.</t>
          </li>
          <li>
            <t>A Connector's tolerance of a read-only inbound tree.</t>
          </li>
          <li>
            <t>Ingest by copy.</t>
          </li>
          <li>
            <t>Write-side path discipline, in all three of its conjunctive legs.</t>
          </li>
          <li>
            <t>Read-side open discipline.</t>
          </li>
          <li>
            <t>Single writer and single drainer per Namespace.</t>
          </li>
          <li>
            <t>Compose-time rejection or stripping of CR, LF, and NUL.</t>
          </li>
        </ul>
        <t>Two of these deserve emphasis as trust-model statements rather than as
testing notes. First, <tt>disposition == "clean"</tt> is the <em>Runtime's
assertion</em>, and this document deliberately does not specify the basis for
it; a consumer that treats <tt>clean</tt> as a proof rather than as an assertion
has misread the trust model. Second, this protocol cannot make a Runtime
trustworthy — it can only ensure that everything the Agent contributes is
inert until a Runtime acts on it, and that whatever the Runtime asserts is
legible and attributable to the Runtime.</t>
      </section>
      <section anchor="sec-11-15">
        <name>The extension space, and outcomes as claims</name>
        <t><tt>outbound/ext/&lt;name&gt;/</tt> (<xref target="sec-4-1"/>) is Connector-owned and Agent-writable,
and this document specifies no shape for anything under it. Its <tt>&lt;name&gt;</tt>
is a stable, opaque Connector id (<xref target="sec-4-1"/>), so a Runtime's
out-of-band agreement with a Connector survives that Connector's
repository being renamed. Nothing security-relevant may be built on it.
Its first use, a per-Notice outcome file, is written by the receiving
Agent's own uid and is therefore forgeable by construction: a receiving
Agent can claim a delivery that did not happen, deny one that did, or
suppress a refusal. An outcome is a statement by the receiving side about
what it did with a Notice; a party MUST NOT treat an outcome as proof of
delivery or of non-delivery, and the Runtime's own delivery record is
authoritative over it. A Runtime that reads outcomes may let them change
what a sender is told or what an operator sees; it MUST NOT let them
change what its own record says happened. There is deliberately no inbound
extension space, because an inbound tree is a Runtime assertion about
origin (<xref target="sec-7-1"/>) and a Connector cannot know what a vendor-named
assertion means.</t>
      </section>
    </section>
    <section anchor="sec-12">
      <name>IANA Considerations</name>
      <t>This document has no IANA actions.</t>
    </section>
  </middle>
  <back>
    <references anchor="sec-combined-references">
      <name>References</name>
      <references anchor="sec-normative-references">
        <name>Normative References</name>
        <reference anchor="RFC2119">
          <front>
            <title>Key words for use in RFCs to Indicate Requirement Levels</title>
            <author fullname="S. Bradner" initials="S." surname="Bradner"/>
            <date month="March" year="1997"/>
            <abstract>
              <t>In many standards track documents several words are used to signify the requirements in the specification. These words are often capitalized. This document defines these words as they should be interpreted in IETF documents. This document specifies an Internet Best Current Practices for the Internet Community, and requests discussion and suggestions for improvements.</t>
            </abstract>
          </front>
          <seriesInfo name="BCP" value="14"/>
          <seriesInfo name="RFC" value="2119"/>
          <seriesInfo name="DOI" value="10.17487/RFC2119"/>
        </reference>
        <reference anchor="RFC8174">
          <front>
            <title>Ambiguity of Uppercase vs Lowercase in RFC 2119 Key Words</title>
            <author fullname="B. Leiba" initials="B." surname="Leiba"/>
            <date month="May" year="2017"/>
            <abstract>
              <t>RFC 2119 specifies common key words that may be used in protocol specifications. This document aims to reduce the ambiguity by clarifying that only UPPERCASE usage of the key words have the defined special meanings.</t>
            </abstract>
          </front>
          <seriesInfo name="BCP" value="14"/>
          <seriesInfo name="RFC" value="8174"/>
          <seriesInfo name="DOI" value="10.17487/RFC8174"/>
        </reference>
        <reference anchor="RFC8259">
          <front>
            <title>The JavaScript Object Notation (JSON) Data Interchange Format</title>
            <author fullname="T. Bray" initials="T." role="editor" surname="Bray"/>
            <date month="December" year="2017"/>
            <abstract>
              <t>JavaScript Object Notation (JSON) is a lightweight, text-based, language-independent data interchange format. It was derived from the ECMAScript Programming Language Standard. JSON defines a small set of formatting rules for the portable representation of structured data.</t>
              <t>This document removes inconsistencies with other specifications of JSON, repairs specification errors, and offers experience-based interoperability guidance.</t>
            </abstract>
          </front>
          <seriesInfo name="STD" value="90"/>
          <seriesInfo name="RFC" value="8259"/>
          <seriesInfo name="DOI" value="10.17487/RFC8259"/>
        </reference>
        <reference anchor="RFC3629">
          <front>
            <title>UTF-8, a transformation format of ISO 10646</title>
            <author fullname="F. Yergeau" initials="F." surname="Yergeau"/>
            <date month="November" year="2003"/>
            <abstract>
              <t>ISO/IEC 10646-1 defines a large character set called the Universal Character Set (UCS) which encompasses most of the world's writing systems. The originally proposed encodings of the UCS, however, were not compatible with many current applications and protocols, and this has led to the development of UTF-8, the object of this memo. UTF-8 has the characteristic of preserving the full US-ASCII range, providing compatibility with file systems, parsers and other software that rely on US-ASCII values but are transparent to other values. This memo obsoletes and replaces RFC 2279.</t>
            </abstract>
          </front>
          <seriesInfo name="STD" value="63"/>
          <seriesInfo name="RFC" value="3629"/>
          <seriesInfo name="DOI" value="10.17487/RFC3629"/>
        </reference>
        <reference anchor="RFC5322">
          <front>
            <title>Internet Message Format</title>
            <author fullname="P. Resnick" initials="P." role="editor" surname="Resnick"/>
            <date month="October" year="2008"/>
            <abstract>
              <t>This document specifies the Internet Message Format (IMF), a syntax for text messages that are sent between computer users, within the framework of "electronic mail" messages. This specification is a revision of Request For Comments (RFC) 2822, which itself superseded Request For Comments (RFC) 822, "Standard for the Format of ARPA Internet Text Messages", updating it to reflect current practice and incorporating incremental changes that were specified in other RFCs. [STANDARDS-TRACK]</t>
            </abstract>
          </front>
          <seriesInfo name="RFC" value="5322"/>
          <seriesInfo name="DOI" value="10.17487/RFC5322"/>
        </reference>
        <reference anchor="RFC2045">
          <front>
            <title>Multipurpose Internet Mail Extensions (MIME) Part One: Format of Internet Message Bodies</title>
            <author fullname="N. Freed" initials="N." surname="Freed"/>
            <author fullname="N. Borenstein" initials="N." surname="Borenstein"/>
            <date month="November" year="1996"/>
            <abstract>
              <t>This initial document specifies the various headers used to describe the structure of MIME messages. [STANDARDS-TRACK]</t>
            </abstract>
          </front>
          <seriesInfo name="RFC" value="2045"/>
          <seriesInfo name="DOI" value="10.17487/RFC2045"/>
        </reference>
        <reference anchor="RFC2046">
          <front>
            <title>Multipurpose Internet Mail Extensions (MIME) Part Two: Media Types</title>
            <author fullname="N. Freed" initials="N." surname="Freed"/>
            <author fullname="N. Borenstein" initials="N." surname="Borenstein"/>
            <date month="November" year="1996"/>
            <abstract>
              <t>This second document defines the general structure of the MIME media typing system and defines an initial set of media types. [STANDARDS-TRACK]</t>
            </abstract>
          </front>
          <seriesInfo name="RFC" value="2046"/>
          <seriesInfo name="DOI" value="10.17487/RFC2046"/>
        </reference>
        <reference anchor="RFC2047">
          <front>
            <title>MIME (Multipurpose Internet Mail Extensions) Part Three: Message Header Extensions for Non-ASCII Text</title>
            <author fullname="K. Moore" initials="K." surname="Moore"/>
            <date month="November" year="1996"/>
            <abstract>
              <t>This particular document is the third document in the series. It describes extensions to RFC 822 to allow non-US-ASCII text data in Internet mail header fields. [STANDARDS-TRACK]</t>
            </abstract>
          </front>
          <seriesInfo name="RFC" value="2047"/>
          <seriesInfo name="DOI" value="10.17487/RFC2047"/>
        </reference>
        <reference anchor="RFC2183">
          <front>
            <title>Communicating Presentation Information in Internet Messages: The Content-Disposition Header Field</title>
            <author fullname="R. Troost" initials="R." surname="Troost"/>
            <author fullname="S. Dorner" initials="S." surname="Dorner"/>
            <author fullname="K. Moore" initials="K." role="editor" surname="Moore"/>
            <date month="August" year="1997"/>
            <abstract>
              <t>This memo provides a mechanism whereby messages conforming to the MIME specifications [RFC 2045, RFC 2046, RFC 2047, RFC 2048, RFC 2049] can convey presentational information. It specifies the "Content- Disposition" header field, which is optional and valid for any MIME entity ("message" or "body part"). [STANDARDS-TRACK]</t>
            </abstract>
          </front>
          <seriesInfo name="RFC" value="2183"/>
          <seriesInfo name="DOI" value="10.17487/RFC2183"/>
        </reference>
        <reference anchor="RFC2231">
          <front>
            <title>MIME Parameter Value and Encoded Word Extensions: Character Sets, Languages, and Continuations</title>
            <author fullname="N. Freed" initials="N." surname="Freed"/>
            <author fullname="K. Moore" initials="K." surname="Moore"/>
            <date month="November" year="1997"/>
            <abstract>
              <t>This memo defines extensions to the RFC 2045 media type and RFC 2183 disposition parameter value mechanisms. This memo also defines an extension to the encoded words defined in RFC 2047 to allow the specification of the language to be used for display as well as the character set. [STANDARDS-TRACK]</t>
            </abstract>
          </front>
          <seriesInfo name="RFC" value="2231"/>
          <seriesInfo name="DOI" value="10.17487/RFC2231"/>
        </reference>
        <reference anchor="JSON-SCHEMA-2020-12" target="https://json-schema.org/draft/2020-12/schema">
          <front>
            <title>JSON Schema: A Media Type for Describing JSON Documents</title>
            <author>
              <organization>JSON Schema</organization>
            </author>
            <date year="2020" month="December"/>
          </front>
        </reference>
      </references>
      <references anchor="sec-informative-references">
        <name>Informative References</name>
        <reference anchor="MCP" target="https://modelcontextprotocol.io/specification/2025-11-25">
          <front>
            <title>Model Context Protocol specification, revision 2025-11-25</title>
            <author>
              <organization>Anthropic</organization>
            </author>
            <date year="2025" month="November"/>
          </front>
        </reference>
        <reference anchor="A2A" target="https://a2a-protocol.org">
          <front>
            <title>Agent2Agent Protocol, v1.0.1</title>
            <author>
              <organization>Linux Foundation</organization>
            </author>
            <date year="2026" month="May"/>
          </front>
        </reference>
        <reference anchor="ACP-MERGER">
          <front>
            <title>Agent Communication Protocol merges into the Agent2Agent project</title>
            <author>
              <organization>LF AI &amp; Data</organization>
            </author>
            <date year="2025" month="August"/>
          </front>
        </reference>
        <reference anchor="ACP-CLIENT" target="https://agentclientprotocol.com">
          <front>
            <title>Agent Client Protocol</title>
            <author>
              <organization>Zed Industries</organization>
            </author>
            <date>n.d.</date>
          </front>
        </reference>
      </references>
    </references>

<section anchor="app-a">
      <name>Submit-Request Schema</name>
      <t><em>(Normative.)</em> The schema below is written in the JSON Schema 2020-12
dialect <xref target="JSON-SCHEMA-2020-12"/> and governs the artifact defined in
<xref target="sec-5"/>. This is the Agent-authored artifact, and its envelope is closed
at every level: an unrecognized member is a hard rejection, surfaced as a
<tt>rejected</tt> Result rather than as a silent drop (<xref target="sec-10-1"/>).</t>
      <figure anchor="schema-submit-request">
        <name>schemas/submit-request.schema.json</name>
        <sourcecode type="json" name="submit-request.schema.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://amap-spec/schemas/submit-request.schema.json",
  "title": "submit-request",
  "description": "agent -> runtime: an inert outbound request. amap-\
    spec v3.0.0 (spec/contract.md §3). Agent-authored: this document\
    's envelope stays CLOSED (additionalProperties:false) at every l\
    evel per §7's v3.0.0 asymmetry — an unrecognized member here is \
    a designed, legible `rejected` refusal, never a silent drop.",
  "type": "object",
  "required": ["contract_version", "req_id", "draft"],
  "additionalProperties": false,
  "properties": {
    "contract_version": { "const": "2" },
    "req_id": {
      "type": "string",
      "minLength": 1,
      "description": "monotonic per namespace; filename-safe (path a\
    uthority is the filename, not this field — §2, the filename-auth\
    ority-for-paths obligation).",
      "pattern": "^[A-Za-z0-9_][A-Za-z0-9_.-]*(?!\\n)$"
    },
    "agent_id": { "type": "string", "minLength": 1, "pattern": "^[^\\
    u0000-\u001f\u007f]*(?!\\n)$" },
    "ts": { "type": "string", "minLength": 1, "pattern": "^[^\u0000-\
    \u001f\u007f]*(?!\\n)$" },
    "in_reply_to": { "type": "string", "pattern": "^[^\u0000-\u001f\\
    u007f]*(?!\\n)$" },
    "draft": {
      "type": "object",
      "required": ["to", "subject", "body_text"],
      "additionalProperties": false,
      "properties": {
        "to": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "one bare addr-spec per element (no displ\
    ay name, address list, quoted local part, comment, IP literal, o\
    r folding whitespace — deliberately narrower than RFC 5322: thos\
    e forms are exactly what make allowlist evaluation non-mechanica\
    l). EAI/SMTPUTF8 is out of scope at this version.",
            "pattern": "^[A-Za-z0-9!#$%&'*+/=?^_`{|}~.-]+@[A-Za-z0-9\
    .-]+(?!\\n)$"
          },
          "minItems": 1
        },
        "cc": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^[A-Za-z0-9!#$%&'*+/=?^_`{|}~.-]+@[A-Za-z0-9\
    .-]+(?!\\n)$"
          }
        },
        "subject": { "type": "string", "pattern": "^[^\u0000-\u001f\\
    u007f]*(?!\\n)$" },
        "body_text": { "type": "string" },
        "reply_to_message_id": { "type": ["string", "null"], "patter\
    n": "^[^\u0000-\u001f\u007f]*(?!\\n)$" },
        "attachments": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["filename", "media_type", "size_bytes", "sh\
    a256"],
            "additionalProperties": false,
            "properties": {
              "filename":   { "type": "string", "minLength": 1, "pat\
    tern": "^[^\u0000-\u001f\u007f]*(?!\\n)$" },
              "media_type": { "type": "string", "minLength": 1, "pat\
    tern": "^[^\u0000-\u001f\u007f]*(?!\\n)$" },
              "size_bytes": { "type": "integer", "minimum": 0 },
              "sha256":     { "type": "string", "pattern": "^[a-f0-9\
    ]{64}(?!\\n)$" }
            }
          }
        }
      }
    }
  }
}
]]></sourcecode>
      </figure>
    </section>
    <section anchor="app-b">
      <name>Result Schema</name>
      <t><em>(Normative.)</em> JSON Schema 2020-12 <xref target="JSON-SCHEMA-2020-12"/>; governs the
artifact defined in <xref target="sec-6"/>. Runtime-authored, and therefore open at
every level: a consumer tolerates unrecognized members (<xref target="sec-10-1"/>). The
<tt>attachments[i].index == i</tt> binding stated in <xref target="sec-6-1"/> is not
expressible here and is enforced as a named post-check by the conformance
gate (<xref target="app-h"/>).</t>
      <figure anchor="schema-result">
        <name>schemas/result.schema.json</name>
        <sourcecode type="json" name="result.schema.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://amap-spec/schemas/result.schema.json",
  "title": "result",
  "description": "runtime -> agent: verdict for a submit-request. am\
    ap-spec v3.0.0 (spec/contract.md §4). Runtime-authored: this doc\
    ument's envelope is OPEN (no additionalProperties:false) at ever\
    y level per §7's v3.0.0 tolerance rule — a consumer MUST ignore \
    members it does not recognize; emitting an unrecognized member r\
    emains producer non-conformance.",
  "type": "object",
  "required": ["contract_version", "req_id", "outcome"],
  "properties": {
    "contract_version": { "const": "2" },
    "req_id": { "type": "string", "minLength": 1 },
    "outcome": {
      "type": "string",
      "enum": ["accepted", "queued_for_human", "rejected"]
    },
    "detail": { "type": ["string", "null"] },
    "reason_code": { "type": "string", "minLength": 1 },
    "ts": { "type": "string", "minLength": 1 },
    "recipients": { "type": "array", "items": { "type": "string" } },
    "message_id": { "type": ["string", "null"] },
    "job_id": { "type": ["string", "null"] },
    "attestation": {
      "type": "object",
      "required": ["form"],
      "properties": {
        "form":   { "type": "string", "enum": ["self-stamp", "verifi\
    able"] },
        "token":  { "type": "string", "minLength": 1 },
        "claims": { "type": "object" }
      }
    },
    "attachments": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["index", "outcome"],
        "properties": {
          "index":   { "type": "integer", "minimum": 0 },
          "outcome": { "type": "string",
                       "enum": ["accepted", "dlp_held", "rejected_ty\
    pe", "too_large"] },
          "detail":  { "type": ["string", "null"] }
        }
      }
    }
  }
}
]]></sourcecode>
      </figure>
    </section>
    <section anchor="app-c">
      <name>Deliver-Notice Schema</name>
      <t><em>(Normative.)</em> JSON Schema 2020-12 <xref target="JSON-SCHEMA-2020-12"/>; governs the
artifact defined in <xref target="sec-7"/>. Runtime-authored and open at every level.
The two <tt>allOf</tt> conditionals implement the clean-gating of <tt>content_ref</tt>
and <tt>sha256</tt> (<xref target="sec-7-2"/>); the <tt>content_ref</tt> index and prefix binding, by
contrast, is not expressible here and is enforced as a named post-check
(<xref target="app-h"/>).</t>
      <figure anchor="schema-deliver-notice">
        <name>schemas/deliver-notice.schema.json</name>
        <sourcecode type="json" name="deliver-notice.schema.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://amap-spec/schemas/deliver-notice.schema.json",
  "title": "deliver-notice",
  "description": "runtime -> connector: one notice per delivered mes\
    sage. amap-spec v3.0.0 (spec/contract.md §5). Runtime-authored: \
    this document's envelope is OPEN (no additionalProperties:false\
    ) at every level per §7's v3.0.0 tolerance rule — a consumer MUS\
    T ignore members it does not recognize; emitting an unrecognize\
    d member remains producer non-conformance.",
  "type": "object",
  "required": ["contract_version", "notice_id", "ts", "kind", "messa\
    ge"],
  "properties": {
    "contract_version": { "const": "2" },
    "notice_id": { "type": "string", "minLength": 1, "pattern": "^[A\
    -Za-z0-9_][A-Za-z0-9_.-]*(?!\\n)$" },
    "ts": { "type": "string", "minLength": 1 },
    "kind": { "const": "deliver" },
    "message": {
      "type": "object",
      "required": ["id", "from", "subject", "preview", "mailbox"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "from": { "type": "string", "minLength": 1 },
        "subject": { "type": "string" },
        "preview": { "type": "string" },
        "mailbox": { "type": "string", "minLength": 1 },
        "task_id": { "type": "string", "minLength": 1 },
        "thread_id": { "type": "string" },
        "in_reply_to": { "type": "string" },
        "references": { "type": "array", "items": { "type": "string\
    " } },
        "sender_standing": {
          "type": "string",
          "description": "advisory, open vocabulary (known, agent-ti\
    er, first-contact, read-only, ...). A consumer MUST tolerate a v\
    alue outside this list, treating it as opaque — never grounds t\
    o drop the notice; a runtime may add a value as a MINOR change (\
    v3.0.0, contract.md §5/§7)."
        },
        "provenance": {
          "type": "string",
          "description": "advisory, open vocabulary (external, inter\
    nal, ...). A consumer MUST tolerate a value outside this list, t\
    reating it as opaque — never grounds to drop the notice; a runti\
    me may add a value as a MINOR change (v3.0.0, contract.md §5/§7)\
    ."
        },
        "verdict": {
          "type": "object",
          "properties": {
            "auth": { "type": "string" },
            "screen": { "type": "string" },
            "entitlement": { "type": "string" }
          }
        },
        "attachments": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["filename", "media_type", "size_bytes", "di\
    sposition"],
            "properties": {
              "filename":    { "type": "string", "minLength": 1 },
              "media_type":  { "type": "string", "minLength": 1 },
              "size_bytes":  { "type": "integer", "minimum": 0 },
              "sha256":      { "type": "string", "pattern": "^[a-f0-\
    9]{64}(?!\\n)$" },
              "disposition": { "type": "string",
                               "enum": ["clean", "stripped", "quaran\
    tined", "unscanned"] },
              "content_ref": { "type": "string",
                               "pattern": "^[A-Za-z0-9_.-]+\\.attach\
    ments/(0|[1-9][0-9]*)(?!\\n)$" }
            },
            "allOf": [
              { "if": { "required": ["content_ref"] },
                "then": { "properties": { "disposition": { "const": \
    "clean" } } } },
              { "if": { "required": ["sha256"] },
                "then": { "properties": { "disposition": { "const": \
    "clean" } } } }
            ]
          }
        }
      }
    }
  }
}
]]></sourcecode>
      </figure>
    </section>
    <section anchor="app-d">
      <name>Inbound-Message Schema</name>
      <t><em>(Normative.)</em> JSON Schema 2020-12 <xref target="JSON-SCHEMA-2020-12"/>; governs the
artifact defined in <xref target="sec-7-3"/>. Runtime-authored and open at every level.
Its Descriptor rules, including the clean-gating conditionals, match the
deliver-notice schema's.</t>
      <figure anchor="schema-inbound-message">
        <name>schemas/inbound-message.schema.json</name>
        <sourcecode type="json" name="inbound-message.schema.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://amap-spec/schemas/inbound-message.schema.json",
  "title": "inbound-message",
  "description": "runtime -> connector: the full delivered message (\
    incl. body_text), spooled at inbound/messages/notice-<id>.json, \
    keyed on the runtime-minted notice-id (never the provider messag\
    e.id). amap-spec v3.0.0 (spec/contract.md §5). Runtime-authored\
    : this document's envelope is OPEN (no additionalProperties:fals\
    e) at every level per §7's v3.0.0 tolerance rule — a consumer MU\
    ST ignore members it does not recognize; emitting an unrecognize\
    d member remains producer non-conformance.",
  "type": "object",
  "required": ["contract_version", "notice_id", "body_text"],
  "properties": {
    "contract_version": { "const": "2" },
    "notice_id": { "type": "string", "minLength": 1, "pattern": "^[A\
    -Za-z0-9_][A-Za-z0-9_.-]*(?!\\n)$" },
    "body_text": { "type": "string" },
    "id": { "type": "string" },
    "date": { "type": "string" },
    "from": { "type": "string" },
    "to": { "type": "string" },
    "cc": { "type": "string" },
    "subject": { "type": "string" },
    "attachments": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["filename", "media_type", "size_bytes", "dispos\
    ition"],
        "properties": {
          "filename":    { "type": "string", "minLength": 1 },
          "media_type":  { "type": "string", "minLength": 1 },
          "size_bytes":  { "type": "integer", "minimum": 0 },
          "sha256":      { "type": "string", "pattern": "^[a-f0-9]{6\
    4}(?!\\n)$" },
          "disposition": { "type": "string",
                           "enum": ["clean", "stripped", "quarantine\
    d", "unscanned"] },
          "content_ref": { "type": "string",
                           "pattern": "^[A-Za-z0-9_.-]+\\.attachment\
    s/(0|[1-9][0-9]*)(?!\\n)$" }
        },
        "allOf": [
          { "if": { "required": ["content_ref"] },
            "then": { "properties": { "disposition": { "const": "cle\
    an" } } } },
          { "if": { "required": ["sha256"] },
            "then": { "properties": { "disposition": { "const": "cle\
    an" } } } }
        ]
      }
    }
  }
}
]]></sourcecode>
      </figure>
    </section>
    <section anchor="app-e">
      <name>Binding-Record Schema</name>
      <t><em>(Normative.)</em> JSON Schema 2020-12 <xref target="JSON-SCHEMA-2020-12"/>; governs the
artifact defined in <xref target="sec-9"/>. This envelope is closed, for the reason
given in <xref target="sec-9-1"/> — an anti-secret-leak lint rather than a
compatibility surface.</t>
      <figure anchor="schema-binding-record">
        <name>schemas/binding-record.schema.json</name>
        <sourcecode type="json" name="binding-record.schema.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://amap-spec/schemas/binding-record.schema.json",
  "title": "binding-record",
  "description": "runtime/provisioner -> volume root: identity.json\
    , the mailbox<->volume binding record. amap-spec v3.0.0 (spec/co\
    ntract.md §9). INFORMATIONAL, NOT AUTHORITATIVE: the volume is a\
    gent-writable, so a consumer MUST treat contents as untrusted da\
    ta and MUST tolerate the file's absence. Unchanged on the additi\
    onalProperties axis at v3.0.0 (§7): this schema stays closed — i\
    ts closure is an anti-secret-leak lint, not a compatibility surf\
    ace.",
  "type": "object",
  "required": ["agent_id", "address", "provisioned_ts"],
  "additionalProperties": false,
  "properties": {
    "contract_version": { "const": "2" },
    "agent_id": { "type": "string", "minLength": 1 },
    "address": { "type": "string", "minLength": 1 },
    "provisioned_ts": { "type": "string", "minLength": 1 },
    "label": { "type": "string" }
  }
}
]]></sourcecode>
      </figure>
    </section>
    <section anchor="app-f">
      <name>Peer-Notice Schema (v3.1.0 DRAFT)</name>
      <t><em>(Normative for the shape it pins; the profile it belongs to is DRAFT.)</em>
JSON Schema 2020-12 <xref target="JSON-SCHEMA-2020-12"/>; governs a peer notice, the
artifact the peer-origin profile places under <tt>&lt;agent-id&gt;/peer/notices/</tt>.
The profile's prose is a companion document (<tt>spec/peer-origin.md</tt>, v3.1.0
DRAFT); at this document version the schema and the <tt>peer-*</tt> fixtures
(<xref target="app-h"/>) are the whole of what this document specifies about it. The
envelope is Runtime-authored and therefore open (<xref target="sec-10-1"/>). A document
with <tt>kind: "peer"</tt> found under <tt>inbound/notices/</tt> fails the deliver-notice
schema (<xref target="app-c"/>), and a document with <tt>kind: "deliver"</tt> found under
<tt>peer/notices/</tt> fails this one; both are dropped under the rule of
<xref target="sec-7-1"/>.</t>
      <figure anchor="schema-peer-notice">
        <name>schemas/peer-notice.schema.json</name>
        <sourcecode type="json" name="peer-notice.schema.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://amap-spec/schemas/peer-notice.schema.json",
  "title": "peer-notice",
  "description": "runtime -> connector: one notice per peer message \
    placed in <agent-id>/peer/notices/. amap-spec v3.1.0 DRAFT, peer\
    -origin profile (spec/peer-origin.md). Same layout as deliver-no\
    tice with kind and mailbox pinned to \"peer\", message.from pinn\
    ed to a bare addr-spec (the one runtime-asserted `from` in the p\
    rotocol), message.id / in_reply_to / references pinned to the no\
    tice_id charset (the peer message id is bare on the wire; its Me\
    ssage-ID encoding exists only inside a mail header, never in an \
    AMAP document), plus the optional runtime-asserted message.sende\
    r_exposure. Runtime-authored: envelope OPEN at every level per c\
    ontract.md §7 — a consumer MUST ignore members it does not recog\
    nize. A document with kind \"peer\" found under inbound/notices\
    / fails deliver-notice.schema.json; a document with kind \"deliv\
    er\" found under peer/notices/ fails this schema: both are dropp\
    ed under §5's drop rule (the profile's second lock).",
  "type": "object",
  "required": ["contract_version", "notice_id", "ts", "kind", "messa\
    ge"],
  "properties": {
    "contract_version": { "const": "2" },
    "notice_id": { "type": "string", "minLength": 1, "pattern": "^[A\
    -Za-z0-9_][A-Za-z0-9_.-]*(?!\\n)$" },
    "ts": { "type": "string", "minLength": 1 },
    "kind": { "const": "peer" },
    "message": {
      "type": "object",
      "required": ["id", "from", "subject", "preview", "mailbox"],
      "properties": {
        "id": {
          "type": "string", "minLength": 1,
          "description": "the peer message id: minted by the sendin\
    g runtime on accepting the submit, returned to the sender as res\
    ult.message_id, >=128 bits of entropy, globally unique and ungue\
    ssable. Bare on the wire under the notice_id charset; when carri\
    ed in a mail header it is encoded as <id@domain> and decoded aga\
    in before it enters any AMAP document.",
          "pattern": "^[A-Za-z0-9_][A-Za-z0-9_.-]*(?!\\n)$"
        },
        "from": {
          "type": "string", "minLength": 1,
          "description": "bare addr-spec, runtime-asserted from the \
    restricted write path or the verified cross-host statement, neve\
    r copied from sender text. The one `from` in the protocol a cons\
    umer may treat as authenticated origin.",
          "pattern": "^[A-Za-z0-9!#$%&'*+/=?^_`{|}~.-]+@[A-Za-z0-9.-\
    ]+(?!\\n)$"
        },
        "subject": { "type": "string" },
        "preview": { "type": "string" },
        "mailbox": { "const": "peer" },
        "task_id": { "type": "string", "minLength": 1 },
        "thread_id": { "type": "string" },
        "in_reply_to": {
          "type": "string", "minLength": 1,
          "description": "present iff the placing runtime resolved i\
    t from its own ledger (a peer message id it issued or placed). R\
    EQUIRED on a result by prose; a fresh task omits it. Bare peer i\
    d, never the Message-ID-encoded form.",
          "pattern": "^[A-Za-z0-9_][A-Za-z0-9_.-]*(?!\\n)$"
        },
        "references": {
          "type": "array",
          "items": { "type": "string", "minLength": 1, "pattern": "^\
    [A-Za-z0-9_][A-Za-z0-9_.-]*(?!\\n)$" }
        },
        "sender_standing": {
          "type": "string",
          "description": "advisory, open vocabulary; SHOULD be `agen\
    t-tier` when set on a peer notice."
        },
        "provenance": {
          "type": "string",
          "description": "advisory, open vocabulary; redundant with \
    the tree on a peer notice and MAY be omitted. Never grounds to d\
    erive or override origin class."
        },
        "verdict": {
          "type": "object",
          "properties": {
            "auth": { "type": "string" },
            "screen": { "type": "string" },
            "entitlement": { "type": "string" }
          }
        },
        "sender_exposure": {
          "type": "object",
          "description": "runtime-asserted, never sender-settable (\
    a submit-request carrying it is `rejected` by the closed envelop\
    e; cross-host it is taken only from the verified statement). Abs\
    ence means NOT ASSESSED, never `no exposure`. MUST NOT drive rou\
    ting or treatment.",
          "required": ["asserted_by", "external_mail_delivered", "wi\
    ndow_start", "window_end"],
          "properties": {
            "asserted_by": { "type": "string", "minLength": 1, "patt\
    ern": "^[A-Za-z0-9!#$%&'*+/=?^_`{|}~.-]+@[A-Za-z0-9.-]+(?!\\n)$\
    " },
            "external_mail_delivered": { "type": "boolean" },
            "window_start": { "type": "string", "minLength": 1 },
            "window_end": { "type": "string", "minLength": 1 },
            "last_external_delivery_ts": { "type": "string", "minLen\
    gth": 1 },
            "derived_from": {
              "type": "array",
              "items": { "type": "string", "minLength": 1, "pattern"\
    : "^[A-Za-z0-9_][A-Za-z0-9_.-]*(?!\\n)$" }
            }
          },
          "if": { "required": ["external_mail_delivered"], "properti\
    es": { "external_mail_delivered": { "const": true } } },
          "then": { "required": ["last_external_delivery_ts"] }
        },
        "attachments": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["filename", "media_type", "size_bytes", "di\
    sposition"],
            "properties": {
              "filename":    { "type": "string", "minLength": 1 },
              "media_type":  { "type": "string", "minLength": 1 },
              "size_bytes":  { "type": "integer", "minimum": 0 },
              "sha256":      { "type": "string", "pattern": "^[a-f0-\
    9]{64}(?!\\n)$" },
              "disposition": { "type": "string",
                               "enum": ["clean", "stripped", "quaran\
    tined", "unscanned"] },
              "content_ref": { "type": "string",
                               "pattern": "^[A-Za-z0-9_.-]+\\.attach\
    ments/(0|[1-9][0-9]*)(?!\\n)$" }
            },
            "allOf": [
              { "if": { "required": ["content_ref"] },
                "then": { "properties": { "disposition": { "const": \
    "clean" } } } },
              { "if": { "required": ["sha256"] },
                "then": { "properties": { "disposition": { "const": \
    "clean" } } } }
            ]
          }
        }
      }
    }
  }
}
]]></sourcecode>
      </figure>
    </section>
    <section anchor="app-directory">
      <name>Peer-Directory Schema (v3.1.0 DRAFT)</name>
      <t><em>(Normative for the shape it pins; the profile it belongs to is DRAFT.)</em>
JSON Schema 2020-12 <xref target="JSON-SCHEMA-2020-12"/>; governs the optional peer
directory defined in <xref target="sec-directory"/>, which a Runtime MAY publish at
<tt>&lt;agent-id&gt;/directory.json</tt>. The document is Runtime-authored and therefore
open (<xref target="sec-10-1"/>), and it is advisory: the schema pins its shape, never
what it predicts.</t>
      <figure anchor="schema-directory">
        <name>schemas/directory.schema.json</name>
        <sourcecode type="json" name="directory.schema.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://amap-spec/schemas/directory.schema.json",
  "title": "directory",
  "description": "runtime -> connector: the router's most recent wor\
    d about which peers this agent may address, published per instan\
    ce at <agent-id>/directory.json. amap-spec v3.1.0 DRAFT (spec/co\
    ntract.md §10). ADVISORY, NEVER AUTHORITATIVE: an address's pres\
    ence is not a prediction that a submit to it will be accepted, n\
    or its absence a prediction of refusal — authorisation is evalua\
    ted by the runtime at submit time and only the result (§4) repor\
    ts what happened. A runtime MUST NOT read this file back as auth\
    orisation; enforcement stays the runtime's own graph check on ev\
    ery message. The file MAY be absent. Runtime-authored: envelope \
    is OPEN at every level per §7's v3.0.0 tolerance rule — a consum\
    er MUST ignore members it does not recognize; emitting an unreco\
    gnized member remains producer non-conformance.",
  "type": "object",
  "required": [
    "router",
    "written_at",
    "delegate_to",
    "mail_to"
  ],
  "properties": {
    "contract_version": {
      "const": "2"
    },
    "router": {
      "type": "string",
      "description": "the publishing runtime's own address, one bar\
    e addr-spec. Never appears in delegate_to or mail_to.",
      "pattern": "^[A-Za-z0-9!#$%&'*+/=?^_`{|}~.-]+@[A-Za-z0-9.-]+(?\
    !\\n)$"
    },
    "written_at": {
      "type": "string",
      "minLength": 1,
      "description": "RFC 3339, the publishing runtime's clock. The \
    only staleness signal a consumer gets; an honest marker, not a f\
    reshness guarantee.",
      "pattern": "^[^\\u0000-\\u001f\\u007f]*(?!\\n)$"
    },
    "delegate_to": {
      "type": "array",
      "description": "sorted addresses this agent may task — the aut\
    horisation graph transposed for this instance. Always present; e\
    mpty when the lane is closed. Never this agent's own address, an\
    d never who may task THIS agent (an allowlist, withheld by const\
    ruction).",
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9!#$%&'*+/=?^_`{|}~.-]+@[A-Za-z0-9.-]+\
    (?!\\n)$"
      }
    },
    "mail_to": {
      "type": "array",
      "description": "sorted addresses on the mutual mail lane. Alwa\
    ys present; empty when the lane is closed, which is the normal c\
    ase for a delegation-only deployment.",
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9!#$%&'*+/=?^_`{|}~.-]+@[A-Za-z0-9.-]+\
    (?!\\n)$"
      }
    }
  }
}
]]></sourcecode>
      </figure>
    </section>
    <section anchor="app-g">
      <name>Golden Examples</name>
      <t><em>(Normative.)</em> The documents below are reproduced byte-verbatim from the
conformance suite (<xref target="app-h"/>). They are a small selection covering each
artifact type and three rejections; the suite itself is the authority. The
selection is configured in <tt>draft/fixtures.toml</tt>, not hardcoded in this
document's build tooling, and one further example — a Binding Record — is
shown inline at <xref target="sec-9"/> instead of being repeated here.</t>
      <section anchor="fx-request-attachments">
        <name>Valid Submit Request with attachments</name>
        <t>Sidecar blobs are named by array index (<tt>req-&lt;id&gt;.attachments/&lt;i&gt;</tt>); no content_ref may appear on a request and the schema rejects one (<xref target="sec-5-1"/>).</t>
        <figure anchor="fx-request-attachments-src">
          <name>fixtures/valid/request-attachments.json</name>
          <sourcecode type="json" name="request-attachments.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{
  "contract_version": "2",
  "req_id": "00000010",
  "agent_id": "agent.research",
  "ts": "2026-07-21T04:10:00Z",
  "draft": {
    "to": ["owner@example.com"],
    "subject": "Report attached",
    "body_text": "See the attached report and notes.",
    "attachments": [
      { "filename": "report.pdf", "media_type": "application/pdf",
        "size_bytes": 48213,
        "sha256": "970e441b6c4ecd37abf2541e1276c680bdec8481cef9368ff\
    1d010fd6b784b07" },
      { "filename": "notes.txt", "media_type": "text/plain",
        "size_bytes": 128,
        "sha256": "087d07499f2c097ac5396af7a0f1ef5e7a977fa91c631ebd1\
    5eb61bb23ef20aa" }
    ]
  }
}
]]></sourcecode>
        </figure>
      </section>
      <section anchor="fx-notice-clean-ref">
        <name>Valid Deliver Notice with a clean attachment Descriptor</name>
        <t>content_ref's prefix equals the enclosing document's own notice_id and its trailing index equals the Descriptor's own array position (<xref target="sec-7-2"/>); a Connector that relays the Notice without ever dereferencing the reference is fully conformant.</t>
        <figure anchor="fx-notice-clean-ref-src">
          <name>fixtures/valid/notice-attachment-clean-ref.json</name>
          <sourcecode type="json" name="notice-attachment-clean-ref.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{
  "contract_version": "2",
  "notice_id": "1721457600000000003-4242",
  "ts": "2026-07-21T04:06:00Z",
  "kind": "deliver",
  "message": {
    "id": "45",
    "from": "colleague@example.com",
    "subject": "Clean attachment (deferred-release wire shape)",
    "preview": "byte-access forward-valid example",
    "mailbox": "inbox",
    "attachments": [
      { "filename": "photo.jpg", "media_type": "image/jpeg",
        "size_bytes": 204800,
        "sha256": "04c22051bc3f76bc44b952d9fdd3e59214662e7b3fd8a84b8\
    9de09352aeb2264",
        "disposition": "clean",
        "content_ref": "1721457600000000003-4242.attachments/0" }
    ]
  }
}
]]></sourcecode>
        </figure>
      </section>
      <section anchor="fx-message-clean">
        <name>Valid Inbound Message</name>
        <t>The body spool for a delivered message is keyed on the Runtime-minted notice_id, never on the provider message.id (<xref target="sec-7-3"/>).</t>
        <figure anchor="fx-message-clean-src">
          <name>fixtures/valid/message-clean.json</name>
          <sourcecode type="json" name="message-clean.json"><![CDATA[
{
  "contract_version": "2",
  "notice_id": "1721457600000000000-4242",
  "id": "42",
  "date": "Mon, 20 Jul 2026 04:00:00 +0000",
  "from": "Alex Doe <owner@example.com>",
  "to": "agent@example.org",
  "subject": "status?",
  "body_text": "hey, how's the run going",
  "attachments": [
    { "filename": "report.pdf", "media_type": "application/pdf",
      "size_bytes": 48213, "disposition": "unscanned" }
  ]
}
]]></sourcecode>
        </figure>
      </section>
      <section anchor="fx-result-attachments-held">
        <name>Valid Result — a policy hold with per-attachment diagnostics</name>
        <t><tt>attachments[i].index == i</tt> on a Result, contiguous and unique over 0..n-1; per-attachment entries are diagnostic detail under one governing top-level outcome and never license a partial send (<xref target="sec-6-1"/>).</t>
        <figure anchor="fx-result-attachments-held-src">
          <name>fixtures/valid/result-attachments-held.json</name>
          <sourcecode type="json" name="result-attachments-held.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{
  "contract_version": "2",
  "req_id": "00000010",
  "outcome": "queued_for_human",
  "detail": "no outbound screener wired; attachments fail-closed to \
    human review",
  "recipients": ["owner@example.com"],
  "message_id": null,
  "job_id": null,
  "attachments": [
    { "index": 0, "outcome": "dlp_held", "detail": "no verdict sourc\
    e" },
    { "index": 1, "outcome": "dlp_held", "detail": "no verdict sourc\
    e" }
  ]
}
]]></sourcecode>
        </figure>
      </section>
      <section anchor="fx-notice-ref-mismatch">
        <name>Invalid Deliver Notice — a content_ref that fails the binding</name>
        <t>The drop rule of <xref target="sec-7-1"/> is two independent checks: this document is schema-valid and is caught only by the content_ref post-check (<xref target="sec-7-2"/>, <xref target="sec-10-3"/>).</t>
        <t>content_ref names a different notice's Sidecar directory and a non-matching index — it is neither <tt>&lt;notice_id&gt;.attachments/0</tt> for its own array position 0, nor prefixed by this document's own notice_id. Schema validation alone reports zero errors; only the content_ref index/prefix post-check catches it.</t>
        <t>The conformance gate's own errors for this document:</t>
        <sourcecode type="text"><![CDATA[
$.attachments[0].content_ref: 'OTHER-NOTICE.attachments/7' !=
  'n-index-mismatch.attachments/0'
]]></sourcecode>
        <figure anchor="fx-notice-ref-mismatch-src">
          <name>fixtures/invalid/notice-attachment-ref-index-mismatch.json</name>
          <sourcecode type="json" name="notice-attachment-ref-index-mismatch.json"><![CDATA[
{
  "contract_version": "2",
  "notice_id": "n-index-mismatch",
  "ts": "2026-07-21T04:06:00Z",
  "kind": "deliver",
  "message": {
    "id": "46",
    "from": "colleague@example.com",
    "subject": "wrong content_ref",
    "preview": "descriptor points at another notice's sidecar",
    "mailbox": "inbox",
    "attachments": [
      { "filename": "photo.jpg", "media_type": "image/jpeg",
        "size_bytes": 204800,
        "disposition": "clean",
        "content_ref": "OTHER-NOTICE.attachments/7" }
    ]
  }
}
]]></sourcecode>
        </figure>
      </section>
      <section anchor="fx-notice-unknown-member">
        <name>Valid Deliver Notice carrying an unrecognized member</name>
        <t>The Runtime-authored envelope is OPEN as of document version 3.0.0: a consumer MUST ignore a member it does not recognize, at every level (<xref target="sec-10-1"/>).</t>
        <figure anchor="fx-notice-unknown-member-src">
          <name>fixtures/valid/notice-unknown-member.json</name>
          <sourcecode type="json" name="notice-unknown-member.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{ "contract_version": "2", "notice_id": "x", "ts": "t", "kind": "del\
    iver", "x_injected": "surprise",
  "message": { "id": "1", "from": "a@b", "subject": "s", "preview": \
    "p", "mailbox": "inbox",
               "x_message_injected": "also surprise" } }
]]></sourcecode>
        </figure>
      </section>
      <section anchor="fx-request-extra-field">
        <name>Invalid Submit Request carrying an unrecognized member</name>
        <t>The Agent-authored envelope stays CLOSED at every level: an unrecognized member is a designed, legible rejected refusal, never a silent drop (<xref target="sec-10-1"/>).</t>
        <t>additionalProperties: false at the top level of the submit-request schema rejects any member this document does not define.</t>
        <t>The conformance gate's own errors for this document:</t>
        <sourcecode type="text"><![CDATA[
$: additional property 'x_injected' not allowed
]]></sourcecode>
        <figure anchor="fx-request-extra-field-src">
          <name>fixtures/invalid/request-extra-field.json</name>
          <sourcecode type="json" name="request-extra-field.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{ "contract_version": "2", "req_id": "00000021", "x_injected": "surp\
    rise",
  "draft": { "to": ["a@b"], "subject": "s", "body_text": "b" } }
]]></sourcecode>
        </figure>
      </section>
      <section anchor="fx-peer-kind-deliver">
        <name>Invalid peer notice — a deliver-kind document under the peer tree</name>
        <t>Origin class comes from the tree a notice arrived in, never from a self-declared field: a kind: "deliver" document found under peer/notices/ fails the peer-notice schema, the profile's cross-lock (spec/peer-origin.md).</t>
        <t>kind is const "peer" in the peer-notice schema; a document carrying "deliver" fails that const check.</t>
        <t>The conformance gate's own errors for this document:</t>
        <sourcecode type="text"><![CDATA[
$.kind: 'deliver' != const 'peer'
]]></sourcecode>
        <figure anchor="fx-peer-kind-deliver-src">
          <name>fixtures/invalid/peer-kind-deliver.json</name>
          <sourcecode type="json" name="peer-kind-deliver.json"><![CDATA[
NOTE: '\' line wrapping per RFC 8792

{ "contract_version": "2", "notice_id": "9f1c2b7e4d3a4f60b8e2c1d5a7f\
    39e04", "ts": "t", "kind": "deliver",
  "message": { "id": "9f1c2b7e4d3a4f60b8e2c1d5a7f39e04", "from": "pl\
    anner@fleet.example", "subject": "s", "preview": "p", "mailbox"\
    : "peer" } }
]]></sourcecode>
        </figure>
      </section>
    </section>
    <section anchor="app-h">
      <name>Conformance Suite</name>
      <t><em>(Normative for the classes of <xref target="sec-10-3"/>; the honesty statement below
is part of the specification.)</em></t>
      <t>The suite consists of golden artifacts under <tt>fixtures/valid/</tt> and
<tt>fixtures/invalid/</tt>, plus a validator, <tt>fixtures/validate.py</tt>.</t>
      <t>At this document version the suite contains <strong>82 fixtures — 31 valid and 51 invalid</strong>.</t>
      <table>
        <thead>
          <tr>
            <th align="left">fixture</th>
            <th align="left">schema</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td align="left">
              <tt>valid/directory-both-lanes.json</tt></td>
            <td align="left">
              <tt>directory.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/directory-delegation-only.json</tt></td>
            <td align="left">
              <tt>directory.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/directory-minimal.json</tt></td>
            <td align="left">
              <tt>directory.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/directory-unknown-member.json</tt></td>
            <td align="left">
              <tt>directory.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/identity-basic.json</tt></td>
            <td align="left">
              <tt>binding-record.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/identity-labeled.json</tt></td>
            <td align="left">
              <tt>binding-record.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/message-clean.json</tt></td>
            <td align="left">
              <tt>inbound-message.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/message-unknown-member.json</tt></td>
            <td align="left">
              <tt>inbound-message.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/notice-attachment-clean-ref.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/notice-attachments-descriptors.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/notice-enriched.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/notice-minimal.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/notice-provenance-unrecognized-value.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/notice-unknown-member.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/peer-attachment-clean-ref.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/peer-exposure-false.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/peer-exposure-true.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/peer-minimal.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/peer-result.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/peer-unknown-member.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/request-attachment-empty-file.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/request-attachments-empty.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/request-attachments.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/request-minimal.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/request-reply.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/result-accepted.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/result-attachments-accepted.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/result-attachments-held.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/result-attachments-rejected-malformed.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/result-queued.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>valid/result-unknown-member.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/directory-display-name-address.json</tt></td>
            <td align="left">
              <tt>directory.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/directory-missing-mail-to.json</tt></td>
            <td align="left">
              <tt>directory.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/directory-version-1.json</tt></td>
            <td align="left">
              <tt>directory.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/identity-extra-field.json</tt></td>
            <td align="left">
              <tt>binding-record.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/identity-missing-address.json</tt></td>
            <td align="left">
              <tt>binding-record.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/message-missing-body.json</tt></td>
            <td align="left">
              <tt>inbound-message.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-attachment-bad-disposition.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-attachment-content-ref-trailing-newline.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-attachment-missing-disposition.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-attachment-ref-index-mismatch.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-attachment-ref-not-clean.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-attachment-sha-not-clean.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-attachment-traversal-ref.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-kind-peer.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-missing-preview.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-notice-id-trailing-newline.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-traversal-noticeid.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-version-1.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/notice-wrong-kind.json</tt></td>
            <td align="left">
              <tt>deliver-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/peer-attachment-ref-index-mismatch.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/peer-exposure-missing-asserted-by.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/peer-exposure-true-missing-last-ts.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/peer-from-crlf.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/peer-from-display-name.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/peer-in-reply-to-encoded.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/peer-kind-deliver.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/peer-mailbox-inbox.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/peer-message-id-encoded.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/peer-notice-id-trailing-newline.json</tt></td>
            <td align="left">
              <tt>peer-notice.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-attachment-bad-sha.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-attachment-content-ref.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-attachment-filename-crlf.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-attachment-media-type-trailing-newline.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-attachment-missing-sha.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-attachment-negative-size.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-empty-to.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-extra-field.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-recipient-display-name.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-recipient-trailing-newline.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-reply-id-crlf.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-reqid-trailing-newline.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-subject-crlf.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-subject-trailing-newline.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-traversal-reqid.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/request-version-absent.json</tt></td>
            <td align="left">
              <tt>submit-request.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/result-attachment-bad-outcome.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/result-attachments-dup-index.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/result-attachments-gap-index.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/result-attestation-missing-form.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/result-bad-attestation-form.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
          <tr>
            <td align="left">
              <tt>invalid/result-bad-outcome.json</tt></td>
            <td align="left">
              <tt>result.schema.json</tt></td>
          </tr>
        </tbody>
      </table>
      <t><strong>Running it.</strong> The validator is stdlib-only and takes no arguments:</t>
      <sourcecode type="text"><![CDATA[
python3 fixtures/validate.py
]]></sourcecode>
      <t>It exits 0 if and only if every fixture under <tt>valid/</tt> passes and every
fixture under <tt>invalid/</tt> fails. It requires no counterpart, no network, and
no dependency: <strong>the fixtures are the other side.</strong></t>
      <t><strong>How a fixture is mapped to a schema.</strong> By filename prefix — <tt>request-</tt> to
the submit-request schema, <tt>notice-</tt> to deliver-notice, <tt>result-</tt> to
result, <tt>message-</tt> to inbound-message, <tt>identity-</tt> to binding-record, and
<tt>peer-</tt> to peer-notice (<xref target="app-f"/>).</t>
      <t><strong>What the validator checks.</strong> Schema validation in the JSON Schema
2020-12 dialect <xref target="JSON-SCHEMA-2020-12"/>, over the subset of keywords these
schemas use (<tt>type</tt>, <tt>const</tt>, <tt>enum</tt>, <tt>minLength</tt>, <tt>pattern</tt>, <tt>minItems</tt>,
<tt>minimum</tt>, <tt>items</tt>, <tt>required</tt>, <tt>properties</tt>, <tt>additionalProperties</tt>,
<tt>allOf</tt>, and a single <tt>if</tt>/<tt>then</tt> pair) — plus two <strong>named post-checks</strong>,
neither expressible in standard JSON Schema:</t>
      <ul spacing="normal">
        <li>
          <t>the Result-side binding <tt>attachments[i].index == i</tt>, contiguous and
unique over <tt>0</tt> through <tt>n-1</tt> (<xref target="sec-6-1"/>);</t>
        </li>
        <li>
          <t>the <tt>content_ref</tt> index and prefix binding on Notice, Inbound Message,
and peer-notice documents: every Descriptor carrying <tt>content_ref</tt> must
equal <tt>&lt;notice_id&gt;.attachments/&lt;i&gt;</tt> for its own array position <tt>i</tt>,
binding both the trailing index and the leading prefix (<xref target="sec-7-2"/>).</t>
        </li>
      </ul>
      <t>A Connector conformance claim requires running <strong>both</strong> the schema
validation and the second post-check, because exactly one invalid
<tt>notice-*</tt> fixture — and its <tt>peer-*</tt> counterpart — is schema-valid on its
own and is caught only by that post-check (<xref target="app-g"/>, <xref target="sec-10-3"/>).</t>
      <t><strong>A note on pattern anchoring, and a portability cost.</strong> Every schema
pattern in this suite that ends in <tt>$</tt> is written as <tt>(?!\n)$</tt>. The reason
is that a bare <tt>$</tt> matches before a single trailing newline in several
widely used regex engines — Python's <tt>re</tt>, Java, and .NET among them —
unlike the ECMA-262 dialect JSON Schema specifies. Since every one of these
patterns exists to forbid exactly the control characters a permissive <tt>$</tt>
would let through at the end of a string, the lookahead closes that gap in
the schemas themselves rather than in any one validator. Seven fixtures pin
the trailing-newline case specifically, distinct from the CRLF fixtures,
which embed <tt>\r\n</tt> and were already rejected even under a permissive
engine.</t>
      <t>The cost is disclosed rather than discovered: <strong>lookahead-less engines
cannot compile these patterns at all.</strong> RE2 (Go's <tt>regexp</tt>), Rust's
<tt>regex</tt> crate, and POSIX ERE reject <tt>(?!</tt> outright, so the three schemas
carrying the guard fail to load rather than validating loosely. Those
engines already treat <tt>$</tt> as strict end-of-string and never had the gap,
so an implementer on one of them should strip the lookahead — not add a
check outside the pattern, which would be both unnecessary and not what
breaks. The failure is loud (a compile error, never silent acceptance).</t>
      <t><strong>What a green run proves.</strong> That every document shape this protocol
defines is accepted or rejected as specified, by an implementation's own
parser, with no counterpart present.</t>
      <t><strong>What a green run does NOT prove.</strong> The following are filesystem and
policy invariants that no document validator can observe. They are
obligations of the conformance classes regardless (<xref target="sec-10-3"/>), and are
checked operationally:</t>
      <ul spacing="normal">
        <li>
          <t>Sidecar file existence — an extra file, a missing file, or a directory
present when the Descriptor array is empty or absent.</t>
        </li>
        <li>
          <t><tt>size_bytes</tt> and <tt>sha256</tt> actually matching the bytes on disk.</t>
        </li>
        <li>
          <t><tt>filename</tt> display-sanitization, and the "never a path" invariant.</t>
        </li>
        <li>
          <t>The binding between an ordinal and a directory entry on disk. The schema
checks only the <em>Descriptor</em> array's own index member, not that
directory entry <tt>N</tt> exists.</t>
        </li>
        <li>
          <t>Inbound Sidecar directories keyed on the Runtime-minted notice
identifier rather than the provider <tt>message.id</tt>.</t>
        </li>
        <li>
          <t>The location of Runtime-private Staging, outside the mounted Volume.</t>
        </li>
        <li>
          <t>Write ordering and commit-sentinel atomicity — Sidecars before
<tt>req-&lt;id&gt;.json</tt>; <tt>.tmp</tt> plus rename.</t>
        </li>
        <li>
          <t>Caps and allowlists — type allowlists, size caps, Volume quotas.</t>
        </li>
        <li>
          <t>The absence of a spooled body for a quarantined or blocked message
(enforcement by absence, <xref target="sec-7-3"/>).</t>
        </li>
        <li>
          <t>A Connector's tolerance of a read-only <tt>inbound/</tt> tree. The prescribed
check is operational, not a fixture: run the Connector against an
inbound tree it cannot write and confirm that it starts, relays every
pending Notice, resolves bodies, and submits outbound requests, without
requiring any inbound write to succeed (<xref target="sec-10-3"/>).</t>
        </li>
        <li>
          <t>Ingest by copy — validation, policy evaluation, and composition running
over a Runtime-private snapshot taken before any of those steps, rather
than re-reading an Agent-writable path (<xref target="sec-4-1"/>).</t>
        </li>
        <li>
          <t>Write-side path discipline, all three conjunctive legs: full
parent-chain resolution through pinned directory descriptors from the
Namespace root; each directory component opened <tt>O_NOFOLLOW|O_DIRECTORY</tt>
at the moment it is added to that chain; and the final component created
<tt>O_EXCL</tt> and never followed, failing closed on <tt>EEXIST</tt> and <tt>ELOOP</tt>. Any
one leg alone is a known-insufficient partial mitigation (<xref target="sec-4-1"/>).</t>
        </li>
        <li>
          <t>Read-side open discipline — per-component no-symlink-follow opens,
non-blocking opens, <tt>fstat</tt>-based regular-file verification through the
open descriptor, and bounded reads and caps on every Agent-influenced
path (<xref target="sec-4-1"/>).</t>
        </li>
        <li>
          <t>Single writer and single drainer per Namespace — an operational and
deployment invariant, such as an OS-level lock held for the process
lifetime (<xref target="sec-4-1"/>).</t>
        </li>
        <li>
          <t>Compose-time header-boundary stripping: even where the schemas'
control-character patterns are bypassed upstream, the Runtime rejects or
strips CR, LF, and NUL before composing the outbound message
(<xref target="sec-5-1"/>).</t>
        </li>
        <li>
          <t>The peer-origin profile's operational obligations (v3.1.0 DRAFT), none
of them visible to a document validator: the write-authority partition —
no component that admits messages from a transport not restricted to
authorised peer senders holds write authority over <tt>peer/</tt>, and the
prescribed check is a write attempt from inside the admitting component
that must fail; intake isolation for shared-transport peer candidates, in
a location that is neither <tt>inbound/</tt> nor <tt>peer/</tt> and unreachable from
any Agent Namespace; verification placement in a component holding peer
write authority that hashes body and attachment bytes itself;
<tt>sender_exposure</tt> never sender-settable; a peer notice carrying
<tt>in_reply_to</tt> only when the placing Runtime resolved it from its own
record, an unresolvable key being refused rather than stripped; and
at-most-once action by the Connector, with an ambiguous target held
rather than fanned out.</t>
        </li>
      </ul>
      <t>Beyond these, the behavioral obligations of Sections <xref format="counter" target="sec-7"/> and
<xref format="counter" target="sec-8"/> — the untrusted-content posture, the treatment of <tt>task_id</tt> as
correlation rather than authorization, and the requirement that verified
sender identity never be taken from <tt>message.from</tt> — sit outside the
fixture gate entirely. They constrain what an implementation <em>does</em> with a
valid document, which no document-level gate can see.</t>
    </section>
    <section anchor="app-i">
      <name>A Deferred Seam-Visible "Handled" Signal (Non-Normative)</name>
      <t><strong>This appendix is non-normative and is not in force at this document
version.</strong> It records the reasoning behind a gap deliberately left open,
not an addition to the protocol. Nothing below is a directory, a member, or
an artifact that a conforming implementation can rely on, and no
requirement keyword appears anywhere in it.</t>
      <t><strong>The gap.</strong> Making inbound consumption-tracking Connector-private
(<xref target="sec-4-1"/>) closes the writability conflict but leaves a knowledge and
capability split unbridged: only the Connector knows that a Notice has
been relayed to its Agent, while on a read-only inbound tree only the
Runtime can delete anything. Neither side alone can turn "consumed" into
"reclaimed."</t>
      <t><strong>A sketch, not a specification.</strong> One shape that would bridge it is a
Connector-written, zero-byte marker per acknowledged Notice, written into
<tt>outbound/</tt>, where the Connector already has write access in every
posture:</t>
      <figure anchor="ex-ack-sketch">
        <name>Sketch only — not part of this protocol</name>
        <sourcecode type="text"><![CDATA[
outbound/acks/notice-<id>   # sketch only - not part of the protocol
]]></sourcecode>
      </figure>
      <t>A Runtime could then delete the acknowledged Notice and its spooled
message, and finally the marker itself. This is presented purely as a
sketch: no directory and no wire member is introduced by this appendix.</t>
      <t><strong>Why a Connector-forgeable signal would be acceptable here.</strong> A
garbage-collection signal is a resource decision rather than a security
decision. An Agent that falsely claims consumption loses only its own
unread mail; one that never claims consumption only bloats its own inbox.
Neither crosses a trust boundary that matters, and neither touches any
record of the exchange the Runtime keeps on its own side. That is what
distinguishes this case from every other "the Agent writes it, so it
cannot be trusted" case in this document — but it is a distinction a
future version would need to state explicitly rather than assume.</t>
      <t><strong>Why a high-water mark cannot substitute.</strong> <xref target="sec-8"/> requires <tt>task_id</tt>
to be unguessable and never sequential, and nothing in this document
orders notice identifiers, so no single "processed through N" cursor is
expressible. Any consumption signal has to be a <em>set</em> of identifiers, or
time-ordered — and time-ordering would require the consumer to process
strictly in delivery order, which nothing today requires.</t>
      <t><strong>The lifecycle this would enable, and its boundaries.</strong> A reliable "is
this one done?" answer is the watermark a real retention lifecycle
promotes against: cycling a working set, rolling handled traffic into
period-compacted archives, then expiring or tiering to cold storage. Three
boundaries would need to hold if this is ever specified.</t>
      <ul spacing="normal">
        <li>
          <t><strong>The seam is not the record.</strong> A Runtime that needs an exchange of
record already keeps one outside this protocol, tamper-evident.
Retention here is about an Agent's access to its own history, not about
the durability of evidence; conflating the two produces an archive that
satisfies neither.</t>
        </li>
        <li>
          <t><strong>An Agent-writable archive is never evidence.</strong> A compacted archive
living under <tt>outbound/</tt>, which is Agent-writable in every posture, can
be rewritten by the Agent it belongs to. Anything meant to be relied on
has to be Runtime-written or kept outside this protocol entirely.</t>
        </li>
        <li>
          <t><strong>It must not grow the read surface.</strong> The read available today is a
point lookup from a notice identifier to a body. "Search my archive" is
a materially larger capability — decompression, indexing, query — for a
deliberately dumb, credential-free relay to take on. If archives are
ever exposed to an Agent, that likely belongs behind a Runtime-served
surface rather than the file-drop seam.</t>
        </li>
      </ul>
      <t><strong>Why this is deferred rather than specified now.</strong> A capability starts
with a change to this document once a second implementation independently
wants it, with members and fixtures proposed together. Today exactly one
reference Connector carries a private workaround for the underlying
problem — a writability-probing fallback with three modes: move, ledger,
and in-memory. There is no second data point yet to generalize from, and
specifying now would freeze one side's convention as the protocol's
answer.</t>
      <t><strong>One honesty note for a future implementer.</strong> The sketch's own
garbage-collection step is not atomic: if a Runtime crashes between
deleting the Notice artifacts and deleting the acknowledgment marker, the
marker is orphaned, pointing at a Notice that no longer exists. Any real
design needs idempotent collection, in which a missing target is not an
error — recorded here so that it is not rediscovered from scratch.</t>
    </section>
    <section numbered="false" anchor="sec-change-log">
      <name>Change Log</name>
      <t><em>(Informative. One entry per document version, newest first, derived from
the versioning record of the protocol contract this document renders.)</em></t>
      <section numbered="false" anchor="sec-draft-the-protocol-is-amap-the-outbound-extension-space-the-peer-origin-profiles-schema">
        <name>3.1.0 (DRAFT) — the protocol is AMAP; the outbound extension space; the peer-origin profile's schema</name>
        <t>An additive <strong>minor</strong> bump; the wire major stays <tt>"2"</tt>.</t>
        <ul spacing="normal">
          <li>
            <t><strong>The protocol is AMAP; the schema <tt>$id</tt> authority is <tt>amap-spec</tt></strong>
(2026-09-15). The protocol formerly called AMP ("Agent Mailbox Protocol")
is renamed AMAP ("Agent Mailbox Access Protocol") because AMP collides
with the IETF DTN working group's adopted <tt>draft-ietf-dtn-amp</tt>; the
specification repository moves from <tt>agent-mailbox-protocol</tt> to
<tt>amap-spec</tt> with it. The six schema <tt>$id</tt> values move from
<tt>https://agent-mailbox-protocol/schemas/…</tt> to <tt>https://amap-spec/schemas/…</tt>.
Nothing on the wire changes shape: no document carries the protocol's name
as a field, and <tt>$id</tt> is an identifier the gate never dereferences.
Implementations that mirror the Runtime's address spell its local part
<tt>amap.router</tt> from this version. This draft's filename is a separate
decision and is unchanged here. This version is marked DRAFT: its profile
prose is a companion document (<tt>spec/peer-origin.md</tt>, itself DRAFT), and
its schema and fixtures may still change before it is settled.</t>
          </li>
          <li>
            <t><strong>Outbound extension space.</strong> <tt>outbound/ext/&lt;name&gt;/</tt> is defined as a
Connector-owned side channel a Runtime MAY read by out-of-band agreement,
with no shape specified and nothing security-relevant permitted to depend
on it (<xref target="sec-4-1"/>). <tt>&lt;name&gt;</tt> is pinned as a stable Connector id —
chosen once, unchanged by any rename of the Connector's repository,
package, or deployment, and opaque to the Runtime that reads it; the
wording it replaced ("a Connector or deployment name") permitted a
repository name to leak into a wire-visible path. The first id is
<tt>claude-code</tt>. One normative clarification sits beside it: an outcome
written there is a claim by the receiving side, never proof of delivery
or of non-delivery, and the Runtime's own delivery record is
authoritative over it (<xref target="sec-11-15"/>). There is deliberately no inbound
extension space.</t>
          </li>
          <li>
            <t><strong>Peer-notice schema.</strong> The peer-origin profile's artifact is pinned by
<tt>peer-notice.schema.json</tt> (<xref target="app-f"/>): the deliver-notice layout with
<tt>kind</tt> and <tt>mailbox</tt> fixed to <tt>peer</tt>, <tt>message.from</tt> a bare addr-spec,
message identifiers in the notice-identifier character set, and the
optional Runtime-asserted <tt>sender_exposure</tt> stamp with its required
members. The profile's prose is the companion document
<tt>spec/peer-origin.md</tt> (v3.1.0 DRAFT).</t>
          </li>
          <li>
            <t><strong>Fixtures: 75 total (27 valid, 48 invalid), up from 58.</strong> 16 <tt>peer-*</tt>
fixtures were added (6 valid, 10 invalid), covering a minimal notice, a
reply, the exposure stamp asserted both ways, a clean attachment
reference, an unknown-member case, and rejections for a <tt>deliver</tt> kind
under the peer tree, a <tt>peer</tt> kind under the inbound tree, a display-name
or CRLF <tt>from</tt>, encoded message identifiers, a missing <tt>asserted_by</tt>, a
missing last-delivery timestamp when exposure is asserted true, an inbox
<tt>mailbox</tt>, a <tt>content_ref</tt> index mismatch, and a trailing-newline
identifier. The validator maps the <tt>peer-</tt> prefix to the new schema and
applies the <tt>content_ref</tt> post-check to it. Three existing fixtures had
example strings normalised to <tt>example.*</tt> identities (one free-text body,
two addresses in injection examples); each stays valid or invalid for the
same reason as before, and no schema changed.</t>
          </li>
        </ul>
      </section>
      <section numbered="false" anchor="sec-direction-aware-tolerance-and-six-closed-safety-holes">
        <name>3.0.0 — direction-aware tolerance, and six closed safety holes</name>
        <t>A <strong>major document bump</strong>; the wire major stays <tt>"2"</tt>. <xref target="sec-10-1"/> states
why the two numbers diverge here, and why bumping the wire would have
caused the exact harm this version exists to repair.</t>
        <ul spacing="normal">
          <li>
            <t><strong>Unknown-member tolerance, direction-aware.</strong> The three Runtime-authored
artifacts — deliver-notice, inbound-message, and result — became <strong>open</strong>
at every level: <tt>additionalProperties: false</tt> was removed from the top
level, from <tt>message</tt>, from Descriptor items, and from <tt>attestation</tt>, and
a consumer must tolerate an unrecognized member. Emitting one remains
producer non-conformance. The Agent-authored submit-request envelope
stays <strong>closed</strong> at every level. Consequence: adding an optional member
to a Runtime-authored artifact is now genuinely additive; adding any
member to the submit-request is a major change.</t>
          </li>
          <li>
            <t><strong>...and for values.</strong> The <tt>enum</tt> constraint was removed from
<tt>message.sender_standing</tt> and <tt>message.provenance</tt>, the two explicitly
advisory vocabularies, and a consumer must tolerate an unrecognized value
in either. <tt>kind</tt>, <tt>outcome</tt> (both levels), and <tt>disposition</tt> remain
closed, enum-locked vocabularies.</t>
          </li>
          <li>
            <t><strong>Ingest by copy.</strong> A Runtime must copy an Agent-authored request and its
Sidecars into private staging before validating, must act only on that
copy, and must not re-read an Agent-writable path after a decision
depending on its content (<xref target="sec-4-1"/>).</t>
          </li>
          <li>
            <t><strong>Address syntax and header-injection discipline.</strong> A new obligation in
<xref target="sec-5-1"/> plus schema patterns on <tt>draft.to</tt>/<tt>cc</tt>, <tt>draft.subject</tt>,
attachment <tt>filename</tt> and <tt>media_type</tt>, <tt>draft.reply_to_message_id</tt>,
<tt>in_reply_to</tt>, <tt>agent_id</tt>, and <tt>ts</tt> — including the two members composed
directly into headers, which earlier schema coverage missed.</t>
          </li>
          <li>
            <t><strong>Path discipline, write- and read-side both.</strong> The write-side rule
became <strong>conjunctive</strong> (<xref target="sec-4-1"/>), and a new read-side rule applies
the equivalent discipline to every Agent-influenceable read. A
<tt>notice_id</tt> character-set pattern was added to the deliver-notice and
inbound-message schemas so that every legal <tt>notice_id</tt> is also a legal
<tt>content_ref</tt> prefix. <strong>Disclosed cost:</strong> <tt>notice_id</tt> was previously
unconstrained, so a Runtime minting identifiers outside the new character
set — an ISO-8601 timestamp, standard base64, a URN, or anything else
carrying punctuation outside it — now emits Notices and Inbound Messages
that fail schema validation on a <em>pattern violation on a required
member</em>, and a conforming Connector drops them. <strong>Migration:</strong> such a
Runtime must re-mint identifiers under the new character set (unpadded
base64url or lowercase hex, for instance) before any Connector it talks
to upgrades, because the failure is otherwise silent on both sides.</t>
          </li>
          <li>
            <t><strong>Single writer, single drainer.</strong> <xref target="sec-4-1"/> now names the
concurrency assumption that every atomicity, commit-sentinel, and
<tt>req_id</tt> rule already depended on.</t>
          </li>
          <li>
            <t><strong><tt>message.from</tt> named untrusted.</strong> The untrusted-content sentence now
covers explicitly the member most likely to be rendered to a human or
used for a trust judgement (<xref target="sec-7-1"/>).</t>
          </li>
          <li>
            <t><strong>Document authority.</strong> The contract, its schemas, and its fixtures are
normative; this rendering is generated from them and is
non-authoritative where they disagree. The conformance-class text was
moved into the contract itself so that the demotion deletes nothing, and
a stray claim of a second, message-specific Sidecar directory was
corrected to match the layout tree.</t>
          </li>
          <li>
            <t><strong>Corrections shipped with this version</strong>, rather than a tidied history:
(i) the write rule described in an earlier revision as "strengthened"
had in fact been weakened — the exclusive-create requirement on the
final component had been deleted rather than supplemented
(<xref target="sec-10-1"/>); (ii) the inbound drop rule described as a pure closure
over schema validation was refuted by this version's own
<tt>content_ref</tt>-binding fixture, and is now stated as two independent
checks (<xref target="sec-7-1"/>, <xref target="sec-10-3"/>); (iii) the claim that this version's
producer bytes are unchanged is explicitly <em>not</em> made — two producer-side
narrowings are disclosed above, each travelling a legible per-artifact
refusal path; and (iv) the 2.1.0 entry below is corrected in place.</t>
          </li>
          <li>
            <t><strong>Fixtures: 58 total (21 valid, 37 invalid), up from 44.</strong> The
unknown-member notice fixture moved from the invalid set to the valid set
and was extended with an unrecognized member inside <tt>message</tt>; valid
unknown-member fixtures were added for result and inbound-message; the
withheld-body invalid fixture was deleted, having become schema-valid
under the relaxed envelope (its name always over-claimed what
<tt>additionalProperties</tt> alone tested); the bad-provenance fixture moved to
the valid set as an unrecognized-value case; an invalid extra-member
submit-request fixture was added to pin the closed side of the asymmetry,
previously untested at the top level; four invalid fixtures were added
for the address-syntax and control-character rules; a traversal
<tt>notice_id</tt> fixture was added; the <tt>content_ref</tt> index-mismatch fixture
was added, and is the one invalid <tt>notice-*</tt> case that is schema-valid on
its own; and six fixtures pin the trailing-newline anchoring case.</t>
          </li>
        </ul>
      </section>
      <section numbered="false" anchor="sec-inbound-byte-access-in-force">
        <name>2.3.0 — inbound byte access in force</name>
        <t>A <strong>minor bump</strong>, prose only: no schema rule and no fixture changed, and
the wire major stayed <tt>"2"</tt>. It resolved an inconsistency carried since
2.0.0. A fixture showing a <tt>clean</tt> Descriptor with <tt>content_ref</tt> and
<tt>sha256</tt> sat in the valid set as a deliberately forward-valid shape, while
the prose forbade any Runtime from producing it — the gate blessed what
the prose forbade. This version sided with the fixtures: the
release-scoped byte embargo was dropped, and the standing rule became the
one stated in <xref target="sec-7-2"/> — a Runtime may publish bytes and emit
<tt>content_ref</tt>/<tt>sha256</tt> only for a Descriptor it asserts <tt>clean</tt>, and a
Runtime that cannot make that determination must not assert <tt>clean</tt>. The
substantive clarification is that <tt>disposition</tt> is the Runtime's
<em>assertion</em>, its basis deliberately unspecified; the older prose presumed
a scanner, which is what made byte access read as a deferred capability
rather than as a policy decision whose wire shape major 2 already carried.
Nothing was tightened, and there is still no outbound <tt>content_ref</tt>.</t>
      </section>
      <section numbered="false" anchor="sec-deletion-rights-read-only-inbound">
        <name>2.2.0 — deletion rights, read-only inbound</name>
        <t>A <strong>permissive minor bump</strong>: no wire member, no schema change, no fixture
changed or added. It (a) declared a read-only inbound tree a valid Runtime
posture and made the inbound <tt>processed/</tt> archive directory optional, (b)
obliged a Connector not to require inbound write access, making
consumption-recording Connector-private, and (c) named deletion rights per
artifact class, along with the two constraints attached to them: a
Connector must not delete or rewrite a published request, and a Connector
exercising its new deletion rights must preserve <tt>req_id</tt> monotonicity by
other durable means. It also pinned three previously implicit
<tt>reason_code</tt> rules: setting one on every Result became a keyworded
SHOULD, and a consumer must tolerate an unrecognized code and must not
infer <tt>outcome</tt> from it.</t>
      </section>
      <section numbered="false" anchor="sec-the-body-spool-and-three-optional-result-members">
        <name>2.1.0 — the body spool, and three optional Result members</name>
        <t>Recorded at the time as an <strong>additive minor bump</strong>: one new optional
spooled artifact (the Inbound Message) and its schema, one new optional
Notice member (<tt>provenance</tt>), and three new optional Result members
(<tt>reason_code</tt>, <tt>attestation</tt>, <tt>ts</tt>). No existing schema gained a required
member, no existing fixture changed shape, and the wire major stayed <tt>"2"</tt>.</t>
        <t><strong>Correction (recorded at 3.0.0).</strong> That "additive minor" label was
unsound as written. Against a strictly validating 2.0.0 consumer — every
schema at that version was closed at every level — a Notice carrying the
new <tt>provenance</tt> member failed schema validation as a <strong>whole document</strong>
and was silently dropped: precisely the failure mode cited as the reason
attachments had required a major bump, reintroduced as a minor. Version
3.0.0 repairs the versioning model rather than relabelling this history,
which is why this entry still records what shipped rather than what would
have been correct.</t>
      </section>
      <section numbered="false" anchor="sec-attachments-and-contractversion-required">
        <name>2.0.0 — attachments, and <tt>contract_version</tt> required</name>
        <t>A <strong>major bump</strong>, moving the wire major from <tt>"1"</tt> to <tt>"2"</tt>. It added
attachment support in both directions — outbound Descriptors with required
<tt>size_bytes</tt> and <tt>sha256</tt> and their Sidecar bytes, inbound Descriptors with
a <tt>disposition</tt> — and made <tt>contract_version</tt> REQUIRED on the
submit-request, deliver-notice, and result artifacts, with no
compatibility mode. The reason the bump had to be major rather than minor
is the reasoning in <xref target="sec-10-1"/>: under the then-universal closed
envelopes, a new member caused a major-1 consumer to reject the <em>whole
document</em>, which inbound is silent total message loss. A version signal,
rather than an unknown-member rejection, is what makes that refusal
legible.</t>
      </section>
      <section numbered="false" anchor="sec-fixture-identifier-normalization">
        <name>Fixture identifier normalization</name>
        <t>Across these versions the fixture suite's identities were normalized to
<tt>example.com</tt> and <tt>example.org</tt> placeholders, and deployment-specific
identifiers were removed from fixture content. This changed no shape and
no expectation: every affected fixture validates exactly as before, and no
identifier removed in that pass is reintroduced by this document.</t>
      </section>
    </section>
  </back>
</rfc>
